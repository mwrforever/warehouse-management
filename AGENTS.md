# 项目技术栈开发宪法（AGENTS.md）

> 本文件是本项目（库存+生产管理系统）技术开发的最高工程规范，适用于 `server/`（Laravel 13 + PHP 8.3 API 服务）与 `web/`（Vue 3 + TypeScript 单页应用）两个子工程。
> 
> 内容汲取业界通行工程规范与本项目历次评审、缺陷修复中沉淀的教训，随项目演进经评审修订。
> 
> **效力**：本文件与用户全局开发规范共同生效，冲突时以更严格者为准。业务规则（单据状态机、字段口径、报表算法）以 `specs/` 设计文档为准，本文件只管工程技术。
> 
> **分级**：【强制】= 违反即视为缺陷，代码评审/CI 必须拦截；【建议】= 默认遵守，偏离需在评审中说明理由。

---

## 一、总则

1.1 【强制】**简单优先**。用最少的代码解决问题，不做推测性设计、不添加未要求的功能与抽象。评审时自问"资深工程师会觉得这过度复杂吗"。

1.2 【强制】**一致性优先**。新代码必须匹配同目录既有代码的风格、惯用法与注释密度，即使与自己偏好不同。

1.3 【强制】**注释写业务意图**。所有注释、日志、文档使用中文；注释说明"为什么"与业务语义，禁止翻译型注释（"设置变量""调用方法"类）与复述代码的注释。核心逻辑（状态变更、金额计算、权限判断、事务、并发控制、第三方调用）必须有行级中文注释。

1.4 【强制】**精准修改**。每处改动必须能追溯到需求；禁止顺手重构无关代码；本次改动产生的废弃代码（失效测试、未用 import、孤儿文件）必须同次提交清理，历史遗留死代码只指出不擅动。

1.5 【强制】**禁止魔法值**。状态码、业务码、单据类型等必须使用命名常量（后端模型常量 `STATUS_*`、前端 `const enum`）或系统字典，禁止在代码中散落裸数字。

1.6 【强制】**编码基线**。全部源文件 UTF-8 无 BOM、LF 行尾；文件末尾保留一个换行符；移除尾随空白与未使用 import。

---

## 二、工程结构与模块边界

### 2.1 双子工程边界

2.1.1 【强制】`server/` 与 `web/` 是独立工程，各自维护依赖清单；禁止跨工程源码级引用（如 web 直接 require server 目录）。

2.1.2 【强制】前端通过 `web/src/api/` 模块调用后端 `/api/v1` 接口；后端通过 API 契约向外部暴露能力。接口契约变更时两端必须同步修改并同次提交。

### 2.2 后端分层（server/app）

分层与依赖方向固定，禁止反向或跨层调用：

| 层    | 位置                     | 职责                                                 |
| ---- | ---------------------- | -------------------------------------------------- |
| 路由   | `routes/api.php`       | 定义 URL、认证与权限中间件，禁止业务逻辑                             |
| 控制器  | `Http/Controllers/Api` | 参数解析、查询聚合与编排（join/where/分页）、调用 Service、统一响应；禁止业务计算 |
| 表单请求 | `Http/Requests`        | 入参校验与授权判断（`authorize`/`rules`）                     |
| 服务   | `Services`             | 核心业务逻辑、事务边界、状态机流转、金额计算                             |
| 模型   | `Models`               | 数据映射、关系、作用域、状态常量；禁止业务逻辑                            |
| 资源   | `Http/Resources`       | 输出结构裁剪与字段格式化                                       |
| 异常   | `Exceptions`           | 业务异常定义（`*Exception`）                               |
| 支撑   | `Support`              | 无状态工具（`ApiResponse`、`DeletionGuard`）               |

2.2.1 【强制】**写操作必须经 Service 并在 Service 内声明事务边界**；控制器禁止 `DB::transaction` 与写库。

2.2.2 【强制】**校验集中在 FormRequest**；禁止控制器内散落大段手工校验（单字段轻量判断除外）。

2.2.3 【强制】禁止 Service 与 Model 之间循环依赖；Service 之间可互相调用，但不得绕过下层直接操作对方的数据表（需经对方 Service 或明确注释说明）。

### 2.3 前端目录约定（web/src）

| 目录            | 职责                                                                                  |
| ------------- | ----------------------------------------------------------------------------------- |
| `api/`        | 按业务领域一个文件（`purchase.ts`、`production.ts`…），统一走 `http.ts` 封装                          |
| `views/`      | 页面组件，按业务模块分目录（`master/`、`purchase/`、`sales/`、`production/`、`system/`）；只编排页面，不含可复用逻辑 |
| `components/` | 跨页面复用的通用组件                                                                          |
| `layouts/`    | 布局骨架                                                                                |
| `stores/`     | Pinia 状态（仅放跨组件共享状态）                                                                 |
| `utils/`      | 纯函数工具（格式化等）                                                                         |
| `styles/`     | 设计令牌与全局样式（`tokens.css`、`main.css`）                                                  |

2.3.1 【强制】新建文件先确定归属目录，归属不明时在评审中提出，禁止随意放置。

2.3.2 【强制】前端组件内禁止裸 axios；一切请求走 `api/` 模块（统一 baseURL、token、响应解包、401 处理）。

---

## 三、命名规范

### 3.1 后端 PHP

3.1.1 【强制】以 PSR-12 + Pint（laravel 预设）+ PHPCS 为准（机器强制，见 §10）；行长软上限 120、硬上限 140。

3.1.2 【强制】类名（含接口/抽象类）`PascalCase`；方法、变量、函数 `camelCase`；常量 `UPPER_SNAKE_CASE`；类成员按 `const → 属性 → 构造 → 方法` 排序。

3.1.3 【强制】模型状态常量以 `STATUS_` 开头（如 `STATUS_APPROVED`）；业务异常类以 `Exception` 结尾；测试方法 `snake_case`（`test_xxx`，豁免 camelCase 检查）。

3.1.4 【建议】布尔属性/方法使用 `is_`/`has_`/`can_` 前缀（字段）或 `isXxx`/`hasXxx`（方法），表意清晰。

### 3.2 前端 TypeScript / Vue

3.2.1 【强制】组件文件 `PascalCase.vue`；组合式函数 `useXxx.ts`；类型/接口 `PascalCase`（接口不加 `I` 前缀）；变量/函数 `camelCase`；常量 `UPPER_SNAKE_CASE`；枚举 `PascalCase`。

3.2.2 【强制】props/emits 必须显式类型声明；事件名用 kebab-case 字符串（`update:modelValue`、`@row-action`），表达动作而非结果。

3.2.3 【强制】禁止 `any`（含隐式 any）；确需逃逸类型时用 `unknown` + 窄化，并注释原因。

### 3.3 数据库

3.3.1 【强制】表名 `snake_case` 复数；字段 `snake_case`；外键 `xxx_id`（如 `supplier_id`）；布尔字段 `is_`/`has_` 前缀；时间字段 `xxx_at`。

3.3.2 【强制】索引命名：普通 `idx_表名_字段`，唯一 `uniq_表名_字段`（多字段按序拼接），禁止无前缀命名。

---

## 四、后端开发规范（Laravel 13 / PHP 8.3）

### 4.1 金额与数值

4.1.1 【强制】**金额一律分单位整数 + bcmath 运算**（`bcmul`/`bcadd`/`bccomp` 2 位小数），禁止 `float`/`double` 参与金额计算；数量为 `decimal(12,2)` 字符串时同样经 bcmath 比较。此为项目铁律，违反即阻断合入。

4.1.2 【强制】浮点参与金额、数量比较或累加的任何写法（`$a * $b`、`>=` 直接比较）均视为缺陷。

### 4.2 查询与性能

4.2.1 【强制】列表接口必须分页（`paginate`/`cursorPaginate`），禁止全量 `get()` 后前端分页；筛选条件用查询构造器参数绑定，禁止字符串拼接 SQL（含 `like` 需手动绑定 `%keyword%`）。

4.2.2 【强制】禁止 N+1：关联数据必须预加载（`with`/`load`）或单条聚合查询；循环内禁止触发查询。

4.2.3 【强制】`select` 显式列出所需列，禁止 `select('*')`；join 时列名前加表前缀（`purchase_orders.id`）。

4.2.4 【建议】大数据量校验/扣减场景用 `whereIn` 批量预取替代循环单查（如领退料校验）。

### 4.3 事务与并发

4.3.1 【强制】多表写操作必须包裹 `DB::transaction`；事务内先锁定再校验后变更（`lockForUpdate`），防止并发超卖/重复扣减。

4.3.2 【强制】**多资源加锁必须全项目锁序一致**（如固定"单据头 → 明细 → 库存"顺序），违反锁序的代码评审直接打回（死锁风险）。

4.3.3 【强制】不可逆或幂等性关键操作（审核、关闭、冲销、删除）必须在 Service 内做状态前置校验，并配合 `DeletionGuard` 等守卫；状态比较用常量 `===`，禁止 `==` 比较状态。

### 4.4 编号、权限与响应

4.4.1 【强制】业务单据编号统一走 `DocumentSequenceService`，禁止手写日期+自增拼接（并发撞号）。

4.4.2 【强制】权限校验通过路由 `permission:xxx` 中间件完成；控制器/服务内自行判断权限仅限中间件无法表达的动态场景，且必须注释原因。

4.4.3 【强制】所有接口统一响应体 `{code, message, data}`：`code=0` 成功；业务失败用非 0 code + 中文 message（HTTP 状态默认 200，由 code 承载业务结果，仅认证失败等语义明确的场景用 401/403）。输出经 `ApiResponse` trait，禁止散装 `response()->json()`。

4.4.4 【强制】业务异常必须抛领域异常（`PurchaseException`、`SalesException`、`ProductionException`、`InventoryException`…），由全局异常处理转统一响应；禁止控制器内 `try { … } catch (Throwable)` 后自拼响应。

---

## 五、前端开发规范（Vue 3 + TypeScript + Element Plus）

5.1 【强制】新代码一律 `<script setup lang="ts">` + 组合式 API；禁止新写 Options API 组件。

5.2 【强制】类型约束由 `vue-tsc` 把关（`npm run build` 内含类型检查），提交前必须通过；禁止用 `@ts-ignore` 绕过（确需时注释原因并在评审说明）。

5.3 【强制】组件通信用 props/emits，禁止跨层级直接操作其他组件内部状态；跨页面共享数据（登录态等）才进 Pinia，页面内部状态用 `ref`/`reactive` 就地管理。

5.4 【强制】样式使用设计令牌（`styles/tokens.css` 变量），禁止硬编码色值/间距；组件样式加 `scoped`；全局样式只放 `styles/`。

5.5 【强制】列表/表单页遵循项目统一模式：筛选栏（含日期范围选择器）→ 表格（`el-table`）→ 分页 → 弹窗表单；数量/金额展示与回填统一走 `utils/format.ts`，禁止页面各自实现格式化。

5.6 【强制】表单校验用 `el-form` rules（必填/类型/范围/精度）；提交按钮 loading 态与重复提交防护；提交成功统一 `ElMessage` 反馈并刷新列表。

5.7 【强制】接口类型定义与后端响应结构一致（后端返回 snake_case 字段，前端类型照此声明）；后端契约变更必须同步更新 `api/` 模块与调用处。

5.8 【建议】列表筛选、弹窗开关等与路由无关的状态留在组件内；只有"登录态、用户信息"这类跨页状态进 store，禁止把 store 当垃圾桶。

---

## 六、数据库规范（MySQL）

6.1 【强制】表结构只经迁移（`database/migrations`）演进，禁止手改库结构、禁止在代码中执行 DDL；迁移文件命名带日期前缀并按业务域分组。

6.2 【强制】建表规约：每表 `id` 主键 + `created_at`/`updated_at`（timestamps）；金额列 `decimal` 或分单位整数（见 §4.1）；文本长度必设上限；`comment` 必填（表与字段）。

6.3 【强制】索引规约：业务上具有唯一性的字段（含多字段组合，如"单号"）必须建唯一索引；索引遵循最左前缀，禁止为 `%xxx%` 模糊查询建索引；禁止冗余索引（已有 `idx_a_b` 再建 `idx_a` 需评审）。

6.4 【强制】SQL 规约：一律经 Eloquent/查询构造器参数绑定；列表必分页（§4.2.1）；聚合用 SQL 聚合函数而非 PHP 循环累加。

6.5 【强制】数据一致性：库存余额、单据数量等关键数据的变动必须"事务 + 行锁 + 前置校验"三步齐全，且回写字段（如 `received_qty`、`issued_qty`）由库存操作方在事务内同步更新，禁止散落在不同事务中。

6.6 【建议】历史单据数据只增不改（业务留痕）；确需修正走冲销/红冲单据，禁止直接 UPDATE 已审核单据的明细。

---

## 七、异常、错误与日志

7.1 【强制】异常分层：业务异常（`*Exception`，携带用户可读中文 message）→ 全局 handler 转统一响应；系统异常向上抛由框架处理并记 error 日志。禁止捕获后静默（空 catch）、禁止捕获后只 `log` 不处理（业务失败必须让调用方感知）。

7.2 【强制】日志分级：接口入口/业务状态变更/DB 写操作记 info；重试触发/参数异常/数据不一致记 warn；系统异常/调用失败记 error（含堆栈）。日志用中文，包含关键业务标识（单号、ID），禁止打印密码、token、完整敏感字段。

7.3 【强制】前端错误处理：统一由 `http.ts` 解包抛错、页面 `ElMessage.error` 展示后端 message；禁止把原始 axios 错误对象直接塞给用户、禁止遗留调试 `console.log`（提交前清理）。

---

## 八、安全规约

8.1 【强制】认证走 Sanctum token，密码一律哈希；授权走 RBAC（路由 `permission:xxx`），新增接口必须声明所需权限，禁止"登录即可访问"的业务接口。

8.2 【强制】**资源归属校验**：操作他人单据/明细前必须校验归属（如委外单所属生产单、退料单关联的领料单），禁止仅凭 ID 信任调用方。

8.3 【强制】批量赋值防护：模型 `$fillable` 白名单 + FormRequest 白名单双层限制；禁止 `$request->all()` 直传模型。

8.4 【强制】注入防护：所有动态条件走参数绑定（§4.2.1）；上传/导入文件做类型、大小、内容校验。

8.5 【强制】敏感信息不出系统边界：日志、响应、前端存储均不得出现密码明文、完整 token、他人敏感数据；种子/示例数据不得使用真实弱口令之外的通用口令，且不得在文档中记录生产凭据。

---

## 九、测试规约

9.1 【强制】测试分层：后端 `tests/Feature`（接口级：路由+权限+状态机+事务回滚）+ `tests/Unit`（服务级：金额计算、状态重算等纯逻辑）；前端 Vitest（组件逻辑、utils）+ Playwright E2E（`web/e2e`，主业务链路，用例带 TC 编号）。

9.2 【强制】覆盖率：核心功能（资金/金额、认证权限、数据一致性/事务并发、对外 API、状态机流转）单元测试 100%，非核心 ≥ 80%；跨层核心链路必须有集成/E2E 覆盖。

9.3 【强制】测试质量：每个用例覆盖正常/边界/异常三类场景之一；命名表达业务意图（`test_审核后禁止删除`、`TC-PRD-09`），禁止 `test1` 类命名与空断言；断言针对业务结果而非实现细节。

9.4 【强制】测试维护：功能改动导致失效的旧测试直接删除（先确认失效确因本次改动）；新功能必须同步交付测试，测试与实现同一次提交。

9.5 【强制】E2E 环境纪律：跑 Playwright 前必须清理残留的 7000/4000 进程（否则脏库导致用例假失败）；E2E 数据自建自清，禁止依赖手工数据。

---

## 十、工程与工具链（CI 门禁）

10.1 【强制】合入主干前必须全量通过，禁止 `--no-verify` 绕过本地钩子：

| 工程     | 命令                                           | 说明                            |
| ------ | -------------------------------------------- | ----------------------------- |
| server | `vendor/bin/pint`                            | 代码风格（laravel 预设）              |
| server | `vendor/bin/phpcs`                           | PSR-12 编码规范                   |
| server | `vendor/bin/phpstan`                         | 静态分析，level 5                  |
| server | `composer test`                              | PHPUnit 全量                    |
| web    | `npm run lint` / `lint:css` / `format:check` | ESLint / Stylelint / Prettier |
| web    | `npm run type-check`                         | vue-tsc 类型检查                  |
| web    | `npm run test:unit`                          | Vitest 全量                     |
| web    | `npm run test:e2e`                           | Playwright 全量                 |

10.2 【强制】本地钩子（husky + lint-staged）：提交前对暂存文件执行 lint + format 修复；push 前执行全量测试。新机器 clone 后必须 `pre-commit install` + `pre-commit install --hook-type pre-push`。

10.3 【强制】提交信息规范：`type(scope): subject`，subject 用中文概括业务意图，正文说明为什么。type 限：`feat`/`fix`/`docs`/`style`/`refactor`/`perf`/`test`/`chore`/`build`/`ci`。

10.4 【强制】分支纪律：`main` 受保护，禁止直接推代码；功能改动建分支（`feat/xxx`、`fix/xxx`），经 PR 评审合入；PR 必须门禁全绿。

10.5 【强制】依赖管理：禁止手改 `vendor/`、`node_modules/`；升级依赖需全量回归；锁定文件（`composer.lock`、`package-lock.json`）与代码同次提交。

---

## 十一、完成标准（Definition of Done）

功能改动满足以下全部条件才算完成：

- [ ] 实现符合本宪法全部【强制】条款，与既有代码风格一致；
- [ ] 中文注释覆盖类/方法/参数/返回值/异常/关键行，注释讲业务意图；
- [ ] 配套测试已交付且全量通过（核心 100% / 非核心 ≥ 80%），失效旧测试已删除；
- [ ] 本改动产生的废弃代码已清理，无死代码残留；
- [ ] §10.1 门禁全绿（后端四件套 + 前端六件套）；
- [ ] 接口契约变更时前端 `api/` 模块与调用处已同步；
- [ ] `specs/` 设计文档与行为不符之处已更新或在 PR 说明中标注。

---

*本宪法修订须经评审，任何条款的豁免须在 PR 说明中记录理由。*
