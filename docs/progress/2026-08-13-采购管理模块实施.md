# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\php-design
- saved_at: 2026-08-13T10:00:00+08:00
- 版本控制提交号: 478ce27（main，已对齐 origin/main；采购模块经 PR #3 rebase 合入）
- 任务一句话描述: 进销存+生产管理系统（Nexus Factory）——按 superpowers SDD 流程实施**采购管理模块**（采购订单→采购入库单两级单据、审核经 InventoryService 加库存、订单状态联动）已完成并全绿合入 main；下一目标为销售管理模块

## 1. 已完成 (Completed) — 简要标记
- [x] 采购管理模块完整实施（13 commits `b5d7860..478ce27`，SDD 流程 8 Task 逐 Task 审查 + 最终全分支审查 fix wave 闭环；PHPUnit 211（592 断言）、E2E 34/34、phpstan 0、phpcs/pint 全绿；云端 CI 四项全绿后 PR #3 rebase 合入）：
  - 后端：4 表迁移（purchase_orders/purchase_order_items/purchase_inbounds/purchase_inbound_items，金额 decimal(14,2) 分单位、received_qty 回写列、order_id/order_item_id 可空 nullOnDelete）+ 4 模型（PurchaseOrder 五态常量+STATUS_LABELS、PurchaseInbound 两态）+ 8 项权限（purchase.order/inbound.list/create/update/delete，group=采购管理；approve/close 复用 update）
  - **PurchaseOrderService**（`lineAmount/calculateTotal` bcmath 整数分运算 + `syncStatus` 订单状态重算：全部行 received>=quantity → 已完成，否则 → 部分入库，仅对已审核/部分入库生效）+ PurchaseOrderController（CRUD/approve/close/available/inbounds；错误码 1301-1306/1311/1312；available 路由须在 {order} 前）
  - **PurchaseInboundController::approve 核心不变式**：事务内「锁单幂等 1310 → 逐行锁订单行+订单头（状态校验/剩余量 bcmath 复核 1308）→ InventoryService::apply 逐行写 purchase_inbound 流水+余额 → 回写 received_qty → syncStatus 重算」任一步失败整体回滚；错误码 1307-1310；fromOrder 预填剩余量（仅已审核/部分入库、剩余>0 行）；**供应商一致性校验（I3）**：订单 supplier_id 与载荷不一致 → 1308
  - **单号持久序列统一**：`DocumentSequenceService` 抽取（`nextNo(type, prefix, persist, legacyMax)`：FOR UPDATE 原子取号/删除不回退/撞号 1062/19 重试/老库 max 衔接），CK/BOM/PO/PI 四号段；**BOM 从 count+1 迁移到持久序列**（修删除后撞号 500，BomTest 删除不回退测试）
  - 技术债清债：inventory_movements.created_at 索引迁移、alerts 补 orderBy、盘点 store/update 重复行（商品×库位）422、盘点 update/destroy 事务锁行复查（1202/1203 防与 approve 竞态）、MovementsView UTC 日期改本地拼接
  - **DeletionGuard 联动补全（最终审查 I1）**：供应商 1109/仓库 1106/库位 1107/商品 1116 四个 destroy 追加采购表引用检查（purchase_inbounds/purchase_inbound_items），各有测试
  - 前端：2 页（`OrdersView.vue` 五态标签/900px 明细弹窗/扫码/详情+入库记录 tab；`InboundsView.vue` 双入口「从订单生成/新 建」/预填/扫码/审核「入库成功，库存已更新」/route.params.id 直达详情）+ `purchaseApi` 16 方法 + `utils/format.ts`（formatYuan 分→元千分位/toLocalDateString）+ 2 路由 + 「采购管理」菜单组 + 设计系统页覆盖 `design-system/nexus-factory/pages/purchase.md`（ui-ux-pro-max 落地）
  - E2E：`web/e2e/purchase.spec.ts` 11 用例（TC-PUR-01~10 + TC-MST-04 1109 供应商删除保护补测）；**playwright.config workers:1**（inventory/purchase 共享库存数据必须串行，已实证并发污染）；inventory.spec TC-INV-03 补验采购入库单号跳转、TC-INV-06 商品 id 动态化；E2E 文档 §5 已回填
- [x] 最终全分支审查修复（2 commits `929f967`+`978c8af`）：C1 入库单 update/destroy 锁行复查 1309、C2 order_id=null 带 order_item_id → 1308、C3 订单 approve/close try/catch 转 1305/1306、C4 quantity/price regex `^-?\d+(\.\d{1,2})?$`（"1e2"→422、负价仍走 1311 业务码）、I1 删除保护补全、I2 入库单空明细 items=[] → 1301（去 required）、I3 供应商一致性
- [x] 期间修正的计划文本缺陷（heredoc 截断/复制粘贴残留均已在 Task 内修复，SDD 逐 Task reviewer 把关）：TC-PUR-05 数据流按实际可达流程调整（**from-order 弹窗无删除明细行能力**，首张入库单必然含全部剩余行——产品决策点，见 2.3）

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 销售管理模块实施（下一目标）
- 目标: 按 `docs/superpowers/specs/` 销售细化 spec 实现销售订单+销售出库单（「制单→审核」两级状态，审核后调 `InventoryService` 生成 sales_outbound 流水扣库存），E2E TC-SAL-xx 全过
- 当前状态: 细化 spec 与 E2E 文档就绪（`docs/superpowers/specs/` 与 `docs/test/` 目录）；**实施计划未创建**；依赖链：基础资料 → 库存 → 采购（已完成）→ 销售
- 涉及文件/产物:
  - `docs/superpowers/specs/2026-08-12-sales-spec.md` [待确认/修改] — 销售模块细化设计（与采购 spec 同目录，文件名以实际为准）
  - `docs/test/2026-08-12-销售管理模块端到端测试.md` [待确认/修改] — 用例验收标准
  - `docs/superpowers/plans/2026-08-12-sales.md` [待创建] — 实施计划（参照采购计划格式：单号基座 → 迁移+模型 → 状态机 TDD → 前端 → E2E）
  - `server/app/Services/InventoryService.php` [修改] — 销售出库审核调用（source_type=sales_outbound，direction=-1，**接口已就绪直接复用**）
  - `server/database/migrations/*` [待创建] — sales_orders/sales_order_items/sales_outbounds/sales_outbound_items（+ 客户表已在基础资料）
- 已锁定决策: 单据「草稿→已审核」两级状态，审核后库存才变动；出库类单据审核时 InventoryService 出库（余额不足抛 InventoryException，调用方转业务码——**参考采购入库审核模式**：事务内锁单幂等 → 锁订单行验剩余 → apply → 回写 → syncStatus 联动）；单号 `SO{yyyyMMdd}-{3位}` 走 DocumentSequenceService（**加 `TYPE_SO` 常量即可，零迁移**）；**客户删除保护 1111 在 sales_orders 表落地后自动生效**（DeletionGuard 已接线）
- 阻塞点/风险: 销售出库超卖语义（订单行剩余量 = quantity - shipped_qty 与库存余额两层校验）；**采购模块已知技术债「订单 update/destroy 无锁草稿检查」（最终审查 out-of-scope）建议销售模块一并修**（模式现成：锁行复查 + try/catch）
- **下一步具体动作**:
  1. 用 superpowers:writing-plans 生成实施计划：`docs/superpowers/plans/2026-08-12-sales.md`（参照采购计划：先核对销售 spec 错误码段 1401-1412，再迁移+模型+状态机 TDD，再前端+Vitest+E2E）
  2. 用 superpowers:subagent-driven-development 执行（每 Task 派发 implementer + reviewer，ledger 在 `.superpowers/sdd/`，用完即删；**派发 implementer 时明确「不要运行 pint/prettier、不要 git add -A」**）
  3. E2E 顺带补测基础资料 **TC-MST-05 客户删除保护（1111）**
- 验证方式: `php artisan test` 与 `npx vitest run` 全绿；E2E TC-SAL 全过（web/e2e/sales.spec.ts）；云端 CI 全绿后 PR 合入 main（见 §4 流程）

### 2.2 生产/报表/仪表盘模块（依次）
- 目标: 按主 spec §11 迭代 4-6 依次实施（生产→报表→仪表盘），每模块走同一 SDD 流程
- 当前状态: 细化 spec + E2E 文档全部就绪（`docs/superpowers/specs/` 与 `docs/test/`）；未开始
- 已锁定决策: 单据「制单→审核」两级状态；单号 `{前缀}{yyyyMMdd}-{3位}` 走 DocumentSequenceService（加 type 常量：mo/ro 等）；生产工单 BOM 展开、工序报工、委外收发、成品入库联动工单完成
- **下一步具体动作**: 按顺序逐模块 writing-plans + subagent-driven-development；**每模块 E2E 顺带补测上游模块因「表未建」跳过的删除保护用例（1113 工序/1121 BOM）**
- 验证方式: 各模块 E2E 文档 TC 全过；跨模块联动（流水单号跳转、预警三级联动、报表口径一致）按各文档 §4 判定

### 2.3 已知技术债（建议按序处理）
- **订单 update/destroy 无锁草稿检查**（`PurchaseOrderController`，最终审查 out-of-scope）：与采购入库单 C1 修复前同类竞态（并发 approve 期间改/删订单可绕过）——**建议销售模块 Task 1 一并修**（模式：事务内 lockForUpdate 复查 STATUS_DRAFT + try/catch 转 1303/1304）
- **from-order 弹窗无删除明细行能力**（产品决策点）：TC-PUR-05 因此偏离 E2E 文档原流程（首张入库单必然含全部剩余行）。是否给「从订单生成」明细加行删除按钮（限定至少保留一行）需产品拍板；若加，E2E 文档 TC-PUR-03/05 可回归原流程
- **from-order 模式扫码框仍渲染**：可混入无 order_item_id 的独立行（后端设计如此——无 order_item_id 行不受订单约束）；建议 `v-if="mode === 'standalone'"` 一行修复
- **E2E 脆弱断言**（deferred）：TC-INV-05/06/07 子串断言与硬编码 id（TC-INV-06 已动态化部分）；TC-INV-03 跳转断言依赖 spec 文件顺序（workers:1 下确定）
- **deferred minors 全集**：见采购计划 ledger（已删）——含 format.ts 无单测、合计 toFixed 求和 vs 行金额分单位舍入差 1 分（纯展示）、approve catch 注释措辞、available() 全量载入内存过滤、ABBA 理论死锁（两入库单明细行序相反，InnoDB 1213 → 500，概率极低）、SupplierTest 测试名过时（test_destroy_succeeds_when_purchase_tables_missing）、InventorySeeder 非幂等（仅 fresh 场景）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本:
  - PHP 8.5.9 NTS x64（已入用户 PATH）、Composer 2.10.2（已入 PATH；**全局镜像为阿里云且不同步 Laravel 13，装包必须官方源**——项目 composer.json 已加 packagist.org 权威源，勿删）、Laravel 13.25.0（server/，Sanctum 4.3.3）、MySQL 8.4（Docker 容器 php-design-mysql，db=php_design/user=php/pass=secret/port=3306）、**bcmath 扩展已验证可用**（金额整数运算依赖）
  - Node 24.18.0（web/，Vue 3.5 + Vite + Element Plus 2.14.4 + Pinia + Vue Router + axios）；Vitest 4 + @playwright/test 1.62（chromium 已装）
  - Python 3.14.6: `D:\code\envs\python\3.14.6\python.exe`（ui-ux-pro-max search.py 需完整路径调用）
  - CI 工具链（均已入库）：server/ — pint 1.30.5、php_codesniffer 4.0.4（PSR-12，phpcs.xml 含 5 条花括号豁免）、larastan 3.10.0 + phpstan 2.2.8（level 5，需 `--memory-limit=1G`）；web/ — eslint 10 + typescript-eslint 8 + eslint-plugin-vue 10（flat config，**无 browser globals：禁用 setTimeout 用 nextTick**）、prettier 3、stylelint 17、husky 9 + lint-staged 17
- 关键配置:
  - `server/.env`（gitignore 不入库）: DB 已配 MySQL php_design；APP_URL=http://localhost:8000
  - `web/vite.config.ts`: 代理 `/api` → `http://localhost:8000`；端口 5173；vitest include 限定 `src/**/*.test.ts`
  - `web/src/main.ts`: Element Plus 已配 zh-cn locale
  - **playwright.config.ts 已设 `workers: 1`**（inventory/purchase 共享库存数据必须串行，勿改回并行）
  - **注意**：playwright webServer 用 sqlite（`server/database/e2e.sqlite`，已 gitignore）自动 `migrate:fresh --seed`；phpunit 测试库为 sqlite（phpunit.xml，**sqlite 外键约束开启**——测试插外键数据须先建真实主数据）；**sqlite 唯一冲突错误码 19 ≠ MySQL 1062**（撞号重试已双码兼容）；**sqlite 将 DECIMAL 编译为 NUMERIC**（Schema 类型断言须兼容 decimal/numeric）
- 安装/构建/启动命令:
  ```bash
  cd D:/code/project/php-design
  docker compose up -d                                   # MySQL 8.4
  cd server && php artisan migrate --seed && php artisan serve   # 后端 :8000
  cd ../web && npm run dev                               # 前端 :5173
  cd server && php artisan test && cd ../web && npx vitest run    # 全量测试
  # CI 门禁（本地模拟云端）：
  cd web && npm run type-check && npm run lint && npm run lint:css && npm run format:check && npm run test:unit
  cd ../server && vendor/bin/phpstan analyse --no-progress --memory-limit=1G && vendor/bin/phpcs -q && vendor/bin/pint --test && php artisan test
  cd ../web && npx playwright test                        # E2E（自动起后端种子库+vite，34 用例，~1.4m）
  ```

## 4. 全局约定 (Locked Conventions)
- **目录结构**: `server/`（Laravel 13 API，`/api/v1/*`，routes/api.php）；`web/`（Vue 3 前端）；`docs/superpowers/specs|plans/`；`docs/test/`（E2E 测试文档）；`docs/progress/`（本进度文档，每模块一份）；`design-system/nexus-factory/`（MASTER.md + pages/{master-data,inventory,purchase}.md 页覆盖）
- **SDD 实施流程（不可跳过）**: 每模块 = writing-plans 生成计划 → subagent-driven-development 逐 Task 执行（TDD、每 Task reviewer、ledger 在 `.superpowers/sdd/`，用完即删）→ E2E 文档全过 → 最终全分支审查 → **PR 合入 main**
- **git 合入流程**: main 分支保护（strict status check「CI 汇总（全部通过）」+ enforce_admins），直接 push main 被 GH006 拒绝——必须：`git checkout -b feat/xxx` → push 分支 → `gh pr create --base main` → `gh pr checks --watch` 云端 CI 全绿 → `gh pr merge --rebase --delete-branch` → 本地 `git fetch && git reset --hard origin/main` 对齐
- **接口契约**: 统一响应 `{code,message,data}`（code=0 成功；422 仅格式层，业务冲突一律走业务码）；错误码：1001-1008 系统管理、1101-1124 基础资料、1201-1206 库存、**1301-1312 采购（1301 空明细/1302 数量≤0/1303 已审核订单不可改/1304 不可删/1305 重复审核/1306 不可关闭/1307 仓库库位必填/1308 超量或订单不可入库/1309 已审核入库单不可改删/1310 重复审核入库单/1311 负价/1312 重复商品）**、1401-1412 销售、1501-1528 生产、1601 报表；权限中间件 `permission:{资源}.{动作}`（approve/close 复用 update）；分页 `{items,total,page,per_page}`；per_page 钳制 `max(1,min(100,(int)$request->input('per_page',10)))`
- **库存引擎（核心不变式）**: 一切库存变动（采购入库/销售出库/领退料/成品入库/委外收发/盘点）**必须**经 `InventoryService::apply`（事务双写流水+余额；movements 每条含 product_id/warehouse_id/location_id/direction(1|-1)/quantity(恒正)/source_type 枚举/source_id/source_no/remark；出库超卖抛 `InventoryException` 第二参数=业务码；余额允许 0 不允许负；流水只增不改不删）；余额=流水求和恒等式
- **单据审核事务模式（采购模块沉淀，销售/生产模块照抄）**: 事务内「锁单行（幂等判重）→ 逐行锁关联单据行（剩余量/状态复核）→ InventoryService::apply（同事务 savepoint）→ 回写累计量 → syncStatus 重算 → 置已审核+操作人+时间」；业务冲突抛模块专属 Exception（第二参数=业务码）由外层 catch 转 fail；**涉及金额用 bcmath 整数分运算**（bcmul/bcadd/bcsub/bccomp，quantity 允许 2 位小数、price 整数分、amount=round(q×p)）；validatePayload 中 quantity/price 加 `regex:/^-?\d+(\.\d{1,2})?$/'`（防科学计数法 500，负值放行到业务码）
- **单号规则**: `{前缀}{yyyyMMdd}-{3位}` 统一走 `DocumentSequenceService::nextNo(type, prefix, persist, legacyMax)`（document_sequences 表，type+date 唯一，FOR UPDATE 原子取号，删除不回退，撞号 1062/19 重试，老库 max 衔接）；已有 type：check/bom/po/pi（销售加 so、生产加 mo 等）；**禁止 count+1**
- **引用保护（DeletionGuard）**: 已生效——inventory_balances（1106/1107）、inventory_movements（1116）、**purchase_orders.supplier_id（1109）、purchase_inbounds supplier/warehouse/location（1109/1106/1107）、purchase_inbound_items.product_id（1116）**；待销售/生产表落地后自动激活——sales_orders.customer_id（1111）、work_order_operations.process_id（1113）、production_orders.bom_id|product_id（1121）
- **RBAC**: 权限只授权给角色；新模块权限追加 RbacSeeder 权限数组（group=模块名），admin 自动全量持有、operator 自动持有全部 `%.list`；前端按钮用 `auth.has()` 门控
- **设计系统**: nexus-factory 为准（主色 #334155、强调绿 #059669、背景 #F8FAFC、Fira Code+Fira Sans）；页面骨架 `.page-card/.page-title/.toolbar/.btn-primary`；按钮文案全角空格「新 建/保 存/编 辑/删 除/审 核/关 闭/查 看/查 询」；金额列 Fira Code 右对齐 ¥（formatYuan 千分位）；订单五态标签（草稿灰/已审核绿/部分入库蓝/已完成深绿 tag-done/关闭红）；弹窗 900px；扫码自动聚焦（v-model + 命中清空/未命中保留）；页覆盖规范见 `pages/{inventory,purchase}.md`
- **用户硬性约束（AGENTS.md）**: 全部注释/日志/文档中文；UTF-8 无 BOM；LF 行尾（.gitattributes 已强制）；无死代码（未使用 import 零容忍）；核心路径（认证/权限/**库存一致性/审核并发/金额 bcmath**）100% 单测；E2E 未过不合并
- **工程纪律（本会话教训）**: 严禁手动运行 pint/prettier（pre-commit 钩子 lint-staged 自动修复属正常）；SDD 派发 implementer 时明确「不要运行 pint/prettier、不要 git add -A」；Task 提交前必跑全量 phpstan（0 错误）；**计划文件生成时用 heredoc 追加大段代码易截断**（Git Bash 限制，改用小步追加或 Write/Edit）；**el-input 组件实例无 value 属性**（扫码取值必须 v-model）；**eslint 无 browser globals**（禁用 setTimeout，用 nextTick）；**edit 流程必须区分 create/update 走不同 API**
- **云端 CI**: `.github/workflows/ci.yml` 在 push main / PR 到 main 时跑后端（Pint→PHPCS→PHPStan→PHPUnit）+ 前端（vue-tsc→ESLint→Stylelint→Prettier→Vitest）+ E2E（Playwright）+ 汇总 job；main 分支保护要求「CI 汇总（全部通过）」strict 检查
- **环境杂项**: 本机偶发内存占用来自 MindSoar 项目的 basedpyright 检查（与 php-design 无关）；`.zcode/` 与 `auth.json` 已 gitignore
