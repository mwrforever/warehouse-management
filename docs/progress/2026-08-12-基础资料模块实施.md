# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\php-design
- saved_at: 2026-08-12T18:15:00+08:00
- 版本控制提交号: d147ede（分支 main，远程 git@github.com:mwrforever/warehouse-management.git 待同步）
- 任务一句话描述: 进销存+生产管理系统（Nexus Factory）——拆分 8 模块细化 spec 与 E2E 测试文档，按 superpowers SDD 流程逐模块实施；系统管理、基础资料模块已完成，下一目标为库存管理模块

## 1. 已完成 (Completed) — 简要标记
- [x] 系统管理模块完整实施（上一会话，18 commits）：认证/RBAC/用户/角色/字典 + 前端基座，PHPUnit 48 + Vitest 14 + E2E TC-SYS-01~12 全绿（详见 `docs/progress/2026-08-12-系统管理模块实施.md`）
- [x] 基础资料模块完整实施（本会话，18 commits `de4185c..d147ede`，已逐 Task 审查 + 最终全分支审查闭环）：
  - 后端：10 张表迁移（categories/units/warehouses/locations/suppliers/customers/processes/products/bom_headers/bom_items）、10 模型、`DeletionGuard` 引用保护守卫（下游模块表未建自动放行，建后自动生效）、RbacSeeder 追加 32 项基础资料权限、MasterDataSeeder 主数据种子
  - API 8 组（错误码 1101-1124 全量）：分类树（两级限制 1124/1101/1102）、单位（1103/1104）、工序（1112/1113）、仓库库位（1105/1106/1107）、供应商（1108/1109）、客户（1110/1111）、商品含扫码（1114/1115/1116/1117/1122）、BOM 单头+明细（1118/1119/1120/1121/1123，启用版本唯一 + 成品行锁串行化 + 单号重试）
  - 前端：8 个 API 封装 + 8 条路由 + 侧边栏「基础资料」菜单组 + 设计系统页覆盖 `design-system/nexus-factory/pages/master-data.md`（ui-ux-pro-max 落地：类型标签语义色/扫枪交互/安全库存前端拦截）；8 个页面（商品/分类树/单位/仓库+库位弹窗/供应商/客户/BOM/工序）
  - E2E 中 TDD 修复 2 个前端缺陷（el-tree-select 缺 node-key、BOM 物料行单位自动带出）+ 最终审查修复 3 项（BOM 并发唯一移入事务+行锁、分类含子节点移动 1124、商品 remark 透传）
- [x] 测试全绿：后端 PHPUnit 111 passed（254 断言）、前端 Vitest 24 passed（7 文件）、E2E TC-MST-01~11 通过（1106 因库存模块未实施按文档约定跳过，注明「库存模块实施后补测」；结果记录在 E2E 文档 §5）
- [x] SDD 实施计划：`docs/superpowers/plans/2026-08-12-master-data.md`（10 Task + 最终审查，含 TDD 测试代码，可复用模板）；SDD 工作区 `.superpowers/sdd/2026-08-12-master-data/` 已按流程删除
- [x] git 规范化：.gitignore 追加 `.zcode/`、`auth.json`（playwright-cli 登录态）、`server/pint.json|phpcs.xml|phpstan.neon`（未提交的工具配置）

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 库存管理模块实施（下一目标）
- 目标: 按 `docs/superpowers/specs/2026-08-12-inventory-spec.md` 实现库存余额/流水引擎（InventoryService）、盘点单全链路、预警；E2E TC-INV-01~12 全过
- 当前状态: 细化 spec 与 E2E 文档就绪；基础资料模块（商品/仓库/库位主数据 + DeletionGuard 已就位，其中 `inventory_balances.warehouse_id|location_id`、`inventory_movements.product_id` 守卫随本模块迁移落地自动激活）；实施计划**未创建**
- 涉及文件/产物:
  - `docs/superpowers/specs/2026-08-12-inventory-spec.md` [修改] — 核心不变式（流水唯一事实来源、事务双写、审核幂等）
  - `docs/test/2026-08-12-库存管理模块端到端测试.md` [修改] — 12 个用例验收标准
  - `docs/superpowers/plans/2026-08-12-inventory.md` [待创建] — 实施计划（参照基础资料模块计划格式）
  - `server/app/Services/InventoryService.php` [待创建] — 全系统唯一库存变动入口（后续采购/销售/生产模块均调用）
  - `server/database/migrations/*` [待创建] — inventory_balances/inventory_movements/inventory_checks/inventory_check_items
- 已锁定决策: 余额=流水求和恒等式；变动一律事务内写 `inventory_movements` + 更新 `inventory_balances`；盘点审核行锁防并发；预警查询时计算不落库；**基础资料模块的 DeletionGuard 将在本模块迁移落地后自动启用删除保护**（1106/1107/1116）
- 阻塞点/风险: 本模块是核心不变式载体——主 spec §10 要求核心功能 100% 单测覆盖；InventoryService 接口签名需在计划中精确定义（供采购/销售/生产模块复用）；**E2E 补测基础资料 TC-MST-03 步骤 6（1106 仓库有库存不可删）**（结果表已注明）
- **下一步具体动作**:
  1. 用 superpowers:writing-plans 生成实施计划：`docs/superpowers/plans/2026-08-12-inventory.md`（参照基础资料模块计划——先迁移+模型+Service 恒等式测试，再盘点 API+TDD，最后前端页面+Vitest+E2E）
  2. 用 superpowers:subagent-driven-development 执行计划（每 Task 派发 implementer + reviewer，ledger 在 `.superpowers/sdd/`，工作区用完即删）
- 验证方式: `php artisan test` 与 `npx vitest run` 全绿；E2E TC-INV-01~12 全过（playwright-cli，dev server :8000/:5173）；恒等式测试（INV-04）+ 盘盈盘亏（INV-08/09）+ 审核幂等（INV-10）

### 2.2 采购/销售/生产/报表/仪表盘模块（依次）
- 目标: 按主 spec §11 迭代 4-6 依次实施（采购→销售→生产→报表→仪表盘），每模块走同一 SDD 流程
- 当前状态: 细化 spec + E2E 文档全部就绪（`docs/superpowers/specs/` 与 `docs/test/`）；未开始
- 已锁定决策: 单据「制单→审核」两级状态（审核后库存才变动）；状态机见各 spec §6；单号规则 `{模块前缀}{yyyyMMdd}-{流水}`；生产工单 BOM 展开、工序报工自动流转、委外发出/回收、成品入库联动工单完成
- **下一步具体动作**: 按顺序逐模块生成计划+执行（依赖链：基础资料→库存→采购/销售→生产→报表→仪表盘）
- 验证方式: 各模块 E2E 文档 TC 全过；跨模块联动（流水单号跳转、预警三级联动、报表口径一致）按各文档 §4 判定；**每模块 E2E 顺带补测上游模块因「表未建」跳过的删除保护用例（1106/1109/1111/1113/1121）**

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本:
  - PHP 8.5.9 NTS x64: `D:\code\envs\php\8.5.9\php.exe`（已入用户 PATH，新终端直接 `php`）
  - Composer 2.10.2: `D:\code\envs\composer\2.10.2\composer.phar`（已入 PATH；**全局镜像为阿里云且不同步 Laravel 13，装包必须官方源**——用全新 COMPOSER_HOME 绕过）
  - Laravel 13.25.0（server/，Sanctum 4.3.3）、MySQL 8.4（Docker 容器 php-design-mysql，db=php_design/user=php/pass=secret/port=3306）、Node 24.18.0（web/，Vue 3.5 + Vite + Element Plus 2.14.4 + Pinia + Vue Router + axios，Vitest 已配 jsdom）
  - Python 3.14.6: `D:\code\envs\python\3.14.6\python.exe`（ui-ux-pro-max search.py 需完整路径调用）
- 关键配置:
  - `server/.env`（gitignore 不入库）: DB 已配 MySQL php_design；APP_URL=http://localhost:8000；APP_KEY 已生成
  - `server/.env.example` [已修复] — DB 块为 mysql 配置（新 clone 直接可用）
  - `web/vite.config.ts`: 代理 `/api` → `http://localhost:8000`；端口 5173
- 安装/构建/启动命令:
  ```bash
  cd D:/code/project/php-design
  docker compose up -d                                   # MySQL 8.4
  cd server && php artisan migrate --seed && php artisan serve   # 后端 :8000
  cd ../web && npm run dev                               # 前端 :5173
  cd server && php artisan test && cd ../web && npx vitest run    # 全量测试
  ```

## 4. 全局约定 (Locked Conventions)
- **目录结构**: `server/`（Laravel 13 API，`/api/v1/*`，routes/api.php）；`web/`（Vue 3 前端）；`docs/superpowers/specs|plans/`（spec 与实施计划）；`docs/test/`（E2E 测试文档，命名 `{yyyy-mm-dd}-{标题}.md`）；`docs/progress/`（本进度文档）；`design-system/nexus-factory/`（MASTER.md + pages/master-data.md 页覆盖）
- **SDD 实施流程（不可跳过）**: 每模块 = writing-plans 生成计划（`docs/superpowers/plans/`）→ subagent-driven-development 逐 Task 执行（TDD、每 Task reviewer、ledger 在 `.superpowers/sdd/`，工作区用完即删）→ E2E 文档全过 → 最终全分支审查 → 推送远程
- **接口契约**: 统一响应 `{code,message,data}`（code=0 成功；401/403/404/422 均 JSON 信封；422 仅格式层，业务冲突一律走业务码）；错误码 1001-1008 系统管理、1101-1124 基础资料、1201-1206 库存、1301-1312 采购、1401-1412 销售、1501-1528 生产、1601 报表；权限中间件 `permission:{资源}.{动作}`；分页 `{items,total,page,per_page}`；per_page 钳制 `max(1,min(100,(int)$request->input('per_page',10)))`
- **RBAC**: 权限只授权给角色；新模块权限追加 RbacSeeder 权限数组（group=模块名），admin 自动全量持有、operator 自动持有全部 `%.list`；前端按钮用 `auth.has()` 门控，权限码必须与后端路由中间件一致（字典项按钮曾因码位错位返工）
- **引用保护（DeletionGuard）**: `App\Support\DeletionGuard::referenced($table,$column,$id)` 统一检查；表未建返回 false 自动放行，下游模块迁移落地后自动生效；守卫表名：inventory_balances.warehouse_id|location_id、purchase_orders.supplier_id、sales_orders.customer_id、work_order_operations.process_id、production_orders.bom_id|product_id、inventory_movements/purchase_order_items/sales_order_items.product_id
- **BOM 并发唯一**: 1120 检查在 DB::transaction 内 + 成品行 `lockForUpdate()` 串行化；单号 `count()+1` 撞 code unique（23000/1062）重试最多 3 次
- **用户硬性约束（AGENTS.md）**: 全部注释/日志/文档中文；UTF-8 无 BOM；LF 行尾（.gitattributes 已强制，实际 blob 已验证为 i/lf）；无死代码（未使用 import 零容忍）；核心路径（认证/权限/库存一致性/金额）100% 单测；E2E 未过不提交合并
- **工程纪律（本会话教训）**: 严禁在仓库运行未配置的格式化工具（pint 曾 3 次产生全仓无关改动被回滚）；SDD 派发 implementer 时明确「不要运行 pint/prettier」；提交前 git status 精确暂存，禁止 `git add -A` 带无关文件（Task 6 曾混入 57 个文件被重置重提）
- **设计系统**: 前端样式以 nexus-factory 为准（主色 #334155、强调绿 #059669、背景 #F8FAFC、Fira Code+Fira Sans），不得退回 Element Plus 默认蓝白；页面骨架 `.page-card/.page-title/.toolbar/.btn-primary`；按钮文案半角空格「新 建/保 存/编 辑/删 除」；类型标签语义色（原料蓝 #3B82F6/半成品琥珀 #D97706/成品绿 #059669）见 `design-system/nexus-factory/pages/master-data.md`
- **单号规则**: `{模块前缀}{yyyyMMdd}-{流水}`（PO/PI/SO/SOUT/MO/PL/RL/OS/OSR/FI/CK 等；BOM 已实现）
- **git**: 提交前确认 `git config user.name`（已设 dev）；完成后 `git push origin main`（远程 git@github.com:mwrforever/warehouse-management.git）
- **环境杂项**: 本机偶发内存占用来自 MindSoar 项目的 basedpyright 检查（与 php-design 无关，任务管理器结束即可）；`.zcode/` 与 `auth.json` 已 gitignore
