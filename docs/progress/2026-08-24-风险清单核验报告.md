# 2026-08-24 风险清单核验报告（清单核验专员 V1）

- 核验日期：2026-08-25
- 核验对象：`docs/bugs/2026-08-24-后端高风险扫描清单.md`（B-1~B-5、B-101~B-108）、`docs/bugs/2026-08-24-前端高风险扫描清单.md`（BF-1~BF-10、Z-1/Z-2）、`docs/bugs/2026-08-24-宪法设计项偏离清单.md`（D-1~D-21、63 条款对照总表、无法验证区 6 条）
- 核验方式：只读核对——逐条读取条目引用的文件:行号比对代码事实；`git show / git log / git blame` 验证引入提交；grep 全库复核计数；未修改任何代码文件、未执行任何 git 写操作、未编写任何临时脚本
- 判定口径：✅ 真实（代码事实与条目描述一致，含对「证据不足」条目的补证坐实）｜ ❌ 伪（代码事实反驳条目）｜ ⚠️ 证据不足（无法从 L1 确认或否认）
- 总体结论：**46 个判定条目中 45 条 ✅、1 条 ⚠️（Z-1）、0 条 ❌**。三份清单的事实密度与行号精度整体很高；发现若干计数/行号级小误（均不推翻条目成立），详见各条目注记与「核验附带发现」区。

---

## 一、后端清单核验（S1 分区：B-1~B-5）

### B-1 草稿工单被委外草稿单「卡死」（工单 update 撞 FK RESTRICT → 500）——✅ 真实

- 核验过程：读取 `server/app/Http/Controllers/Api/OutsourcingController.php`（store :107-176、approve :333-431）、`server/app/Http/Controllers/Api/ProductionOrderController.php`（update :293-392、destroy :395-423）、`server/database/migrations/2026_08_14_110000_create_production_documents_tables.php`、`server/bootstrap/app.php`、`server/routes/api.php:305-312`；`git show 03d2ef8 / 6df3ab8 / 488bf3c`。
- 三点证据全部坐实：
  1. 委外 store 仅校验工序归属（:120-123）、委外标记（:128-130）、节点未完成（:131-133）、1520 剩余量（:134-139），**无工单状态校验**；approve 则有 `[RELEASED, PRODUCING]` 校验（:359-362），对照成立。
  2. 工单 update 在事务内 `$locked->operations()->delete()` 全删重建（:352），**无任何 OutsourcingOrder 引用检查**；同文件 destroy 有领料/退料/委外/成品入库四表引用检查（:408-411），对照成立。
  3. `outsourcing_orders.operation_id` 为 `restrictOnDelete`（迁移 :70），对照 `operation_reports.operation_id` cascade（见下）。
- git 取证：`6df3ab8`（2026-08-13 生产工单 API）、`488bf3c`（2026-08-13 生产单据域 8 表，diff 含 `operation_id ... restrictOnDelete`）、`b03b989`（PR#32 DAG，工单创建落 `is_outsourced` 快照于 :155-165）均存在且内容相符；`git show 03d2ef8` 证实该修复波对 OutsourcingController 仅 +4 行（storeReceipt 的工单状态校验），**未覆盖委外 store**。
- 异常路径佐证：update 仅 `catch (ProductionException)`（:386-389）；`bootstrap/app.php` 异常渲染器仅覆盖 Authentication/Authorization/NotFound/Validation 四类，**无 QueryException 业务渲染** → FK 撞 restrict 抛 QueryException 必 500，触发链成立。
- 路由佐证：`routes/api.php:309` 确为 `permission:production.outsource.create`，无其它状态门禁。

### B-2 库存引擎数量运算全链路浮点（违反 §4.1 铁律）——✅ 真实

- 核验过程：通读 `server/app/Services/InventoryService.php` 全文 + `OutsourcingController.php:399/:647`。
- 逐点坐实：`:48` `$quantity = (float) $m['quantity'];`；`:58` 出库充足性浮点 `<` 直接比较；`:91-95` 余额浮点累加落库（`$balance->quantity = (float) $balance->quantity + $direction * $quantity;`）；`:104` `balance_after` 同源浮点。上游配合强转：`OutsourcingController.php:399`（`(float) $item->required_qty`，注释自认「引擎签名 quantity 为 float（历史契约……偏离记录⑤）」）与 `:647`（`(float) $l['quantity']`）均原文一致。
- 与 B-107 的分工核实：B-107 收录点位全部在控制器层（见下），确与引擎核心三处不重叠，两互补不重复的声明成立。

### B-3 库存余额行锁序不统一 + 审核事务无死锁重试——✅ 真实

- 核验过程：通读 `InventoryService.php:37-55`（apply 按调用方给定顺序逐行 `lockForUpdate`，无排序）；grep 全部 `DB::transaction` 与 `}, 2);`；读取 PickList/SalesOutbound/PurchaseInbound/ReturnList/Check 各审核段与 `OperationReportController.php:30-33`。
- 三种调用风格并存坐实：领料/销售出库 whereIn 语句预锁（PickListController :307-313、SalesOutboundController :354-360）；委外发出/余料退回 `sortBy('material_id')` 升序（OutsourcingController :380-384、:640）；采购入库（:363-398 movements 随 items 序）、退料（ReturnListController :273-294）、盘点（CheckController :271-284 循环内逐行锁）不排序。
- attempts 全景坐实：审核事务 PurchaseInbound:341 / SalesOutbound:331 / PickList:281 / ReturnList:248 / FinishedInbound:259 / Check:258 均为裸 `DB::transaction(closure)`；带 `, 2)` 的共 16 处——各控制器建单路径（Bom:78、Check:147、FinishedInbound:137、PickList:160、Product:134、ProductionOrder:188、PurchaseInbound:196、SalesOrder:144、SalesOutbound:205、PurchaseOrder:143、ReturnList:127、RoutingService:242）+ 委外四处（OutsourcingController :169/:421/:564/:698），与条目「仅建单路径与委外四处带 , 2」表述一致。
- 项目自证坐实：`OperationReportController.php:30-33` 注释原文含「DB::transaction 默认不重试死锁败方（500），故必须全集升序单条获取」。
- 死锁发生本身为 InnoDB 标准行为推演，本地 SQLite 无法复现行锁语义（与条目自述一致），代码级事实全部确认。

### B-4 BOM 非整除折算舍入口径（bcdiv scale=4 截断）是否有 spec 明文——✅ 事实坐实（「截断是否有意」仍需产品确认）

- 补证过程：读取 `server/app/Services/ProductionOrderService.php:26-30`——`bcmul(bcdiv($quantity, (string) $bom->quantity, 4), (string) $i->quantity, 2)` 与注释「bcmath 4 位中间精度防误差，最终 2 位」逐字相符；全量检索 `specs/` 14 个文件（关键词：舍入/截断/中间精度/折算/精度/小数），**未检索到任何对 BOM 折算舍入方向或中间精度的明文规定**（命中的「折算」均属委外组件/工序数量闭合语义，与舍入无关）。
- 结论：条目提出的疑点已被 L1 证据坐实为真实缺口——该舍入行为无 spec 依据。至于「截断而非进位是否为有意口径」属于产品决策，无法从代码确认，仍需 spec 作者定夺（此为条目本意，维持其待裁决定位，但事实部分全部核实）。

### B-5 前端委外入口是否有「仅已下达工单可建委外单」的 UI 状态过滤——✅ 补证坐实（前端确有状态门控）

- 补证过程：读取 `web/src/views/production/OrdersView.vue` 与 `web/src/views/production/OutsourcingsView.vue`。
- 证据一（列表入口）：OrdersView 操作列「委 外」按钮位于 `<template v-if="row.status === 2">` 块内（:485-514）——**仅生产中工单展示**，跳转 `?order_id=`（:512）。
- 证据二（新建弹窗下拉）：OutsourcingsView onMounted `productionApi.orders({ status: 2, per_page: 100 })`（:593，注释「仅生产中工单可委外」）——**下拉无法选到草稿工单**。
- 证据三（工单详情工序网络）：其「委外单」弹窗仅按工单+工序过滤展示既有委外单（onOutsourceClick :337-345），不提供对草稿工单的建单入口。
- 结论：B-1 触发链经 UI 常规路径的触达概率**显著降低**（草稿工单在两处入口均不可选）；剩余可达路径为 API 直调或手工构造 `/production/outsourcings?order_id=<草稿工单id>` URL（openCreate(orderId) 会预填任意工单）。B-1 作为 API 层缺陷仍然成立，但优先级可据此下调——与条目预判一致。

---

## 二、后端清单核验（S2 分区：B-101~B-108）

### B-101 委外回收记录列表 N+1——✅ 真实

- 核验过程：读取 `OutsourcingController.php:577-596` 与 `:711-736`；`git log -S "function receipts"`。
- 坐实：receipts 分页查询（:579）无 `with` 预载，map 闭包内 `$r->warehouse?->name`（:587）与 `$r->location?->name`（:589）逐行懒加载；同文件 returnList（:713-716）显式 `->with(['material','warehouse','location'])`，同构对照成立。引入提交 `45638d0`（2026-08-13 委外加工 API）确认存在。

### B-102 宪法 §2.2.1 与实现系统性冲突（13 控制器事务+写库）——✅ 真实

- 核验过程：`grep -rn "DB::transaction" app/Http/Controllers/Api/` 全量比对。
- 坐实：**13 个文件**与条目所列完全一致（Bom/Check/PurchaseOrder/PurchaseInbound/SalesOrder/SalesOutbound/ProductionOrder/PickList/ReturnList/Outsourcing/FinishedInbound/OperationReport/Product）；条目所列各文件行号逐一核对**全部命中**（BomController :54/:86/:133、CheckController :128/:212/:237/:258、PurchaseOrderController :117/:191/:229/:252/:278、PurchaseInboundController :171/:278/:317/:341、SalesOrderController :118/:192/:230/:253/:279、SalesOutboundController :180/:267/:306/:331、ProductionOrderController :105/:304/:401/:449/:504/:548/:587（7 处）、PickListController :135/:221/:257/:281/:381、ReturnListController :105/:189/:224/:248、OutsourcingController :144/:271/:309/:339/:455/:614（6 处）、FinishedInboundController :116/:201/:235/:259、OperationReportController :51、ProductController）。
- 两处小误（不推翻结论）：① ProductController 实际行号为 :103（条目写 :101）；② 事务调用点实测 53 处（条目与 D-1 写 51 处；grep 原始 54 处中含 OperationReportController :33 的一处注释文本）。事务闭包内直接写库（create/update/delete/createMany）经抽查属实。

### B-103 BOM/工艺路线 toggle 启用无锁并发窗口——✅ 真实

- 核验过程：读取 `BomController.php`（toggle :128-144、store :46-79）与 `RoutingService.php`（toggle :252-261、persist :181-243）、`ProductionOrderController.php:109/:116`。
- 坐实：BomController toggle 事务内直接 update（:133-141），**无 Product 行锁**；store 先 `Product::whereKey(...)->lockForUpdate()->first()`（:56）再查 hasEnabledVersion（:57-59）。RoutingService toggle 同构无锁（:254-260）；persist 先锁成品行（:185）再查 1707（:186-192）。缓解因素核实：ProductionOrderController 取启用版本用 `orderByDesc('id')->first()`（:109/:116）确定性兜底属实。

### B-104 委外审核余额行循环内逐行锁查询——✅ 真实

- 核验过程：读取 `OutsourcingController.php:374-409`；`git blame -L 380,384`。
- 坐实：`foreach ($locked->items->sortBy('material_id') as $item)` 内逐组件 `InventoryBalance::where(...)->lockForUpdate()->first()`（:380-384），blame 确认全部为 `d7d3acb`（2026-08-24 PR#33）引入；批量预锁对照模式（PurchaseInbound :352-361、PickList :300-313、SalesOutbound :344-360，均含「宪法 §4.2.4 建议」注释）属实。`sortBy('material_id')` 升序保证锁序稳定、并发安全不受影响的论证与代码一致。

### B-105 销售 validateBusinessItems 循环内 Product::find——✅ 真实

- 核验过程：读取 `SalesOrderController.php:354-366`、`SalesOutboundController.php:486-496`；`git blame`。
- 坐实：两处 foreach 内 `Product::find($item['product_id'])` 分别位于 SalesOrderController :362、SalesOutboundController :493（原料禁售校验），`3b64ec94`（2026-08-13）引入确认；BomController :169 `Product::whereIn('id', $materialIds)->get()` 批量对照属实。

### B-106 available 下拉全量装载——✅ 真实

- 核验过程：读取 `PurchaseOrderController.php:84-93`、`SalesOrderController.php:85-94`；`git log -S`。
- 坐实：两处 `whereIn('status',[已审核,部分])->with(['supplier|customer','items'])->get()` 全量取单后 PHP `filter(剩余量>0)`；引入提交 `1bf51aa`（采购订单 API）确认存在。

### B-107 浮点比较/累加息律违反散布（两族）——✅ 真实

- 核验过程：`grep -rn "(float)" app/Http/Controllers/Api/` 全量比对 + 逐点抽读 + `git blame`。
- 族 a 全部 18 个点位逐一命中：OutsourcingController :110/:241/:447、OperationReportController :43/:46、ProductionOrderController :96/:300、PurchaseOrderController :354/:357、SalesOrderController :355/:358、SalesOutboundController :486/:489、PickListController :428、ReturnListController :347、FinishedInboundController :351、CheckController :106/:193。
- 族 b 全部命中：CheckController :273（`$diff = round((float)$actual - (float)$book, 2)` 落库 diff_qty）、:288（`abs(...) > 0.005` 一致性比较）、:298（`abs($diff)` 传流水 quantity）、:306/:309（`$increased/$decreased += abs($diff)` float 累加）。
- git 归属：`e6c3d21c`（:273/:106）、`3037583e`（:43）blame 确认，PR#32/#33 未新增此类写法的论断与 blame 结果一致。
- 注：「两族约 19 个核心点位」的约数与实测（族 a 18 + 族 b 5）存在小偏差，属概括性计数，不影响成立。

### B-108 盘点审核无死锁重试 + 明细序逐行锁——✅ 静态事实坐实（死锁实测不可本地复现；一处表述小误）

- 核验过程：读取 `CheckController.php:253-331`；grep attempts 全景。
- 坐实：`:258` `DB::transaction(...)` 无 attempts 参数；余额行在 `foreach ($locked->items as $item)` 循环内逐行 `lockForUpdate()`（:280-284），锁序由明细插入序决定——两点核心事实与条目描述一致。本地测试套件 SQLite 无法复现 InnoDB 行锁语义（条目自述的限制成立），死锁「实际发生」维持理论级（同 B-3 的置信度口径）。
- 表述小误（不推翻结论）：条目称「同项目多数审核路径 attempts=2，如 OutsourcingController :339、PickListController :135」——实测 attempts=2 的 16 处中**除 OutsourcingController :339-421（approve）外全部为建单/persist 路径**（PickListController :135-160 是 store 而非审核），其余六个审核事务（含 CheckController :258）均无 attempts。「多数审核路径 attempts=2」应为「建单路径普遍 attempts=2、审核路径仅委外 approve 有」，与 S1 B-3 的全景描述（正确）不一致。

---

## 三、前端清单核验（BF-1~BF-10、Z-1/Z-2）

### BF-1 委外页不消费 keyword 参数——✅ 真实

- 核验过程：读取 `OrdersView.vue:827-856`、`OutsourcingsView.vue:54-59/:587-602/:607-610`；全库 grep `route.query`；读取 `e2e/outsourcing.spec.ts:649-672`、`MovementsView.vue:183-196`。
- 坐实：跳转发起 `router.push(\`/production/outsourcings?keyword=${row.no}\`)` 在 :850；OutsourcingsView onMounted 仅读 `route.query.order_id`（:598-601），`query.keyword` 只经 `v-model:keyword`（:607）绑定筛选栏输入框，**无任何页面从 route.query 初始化 keyword**（grep 全库仅 MovementsView:185 消费 product_id/warehouse_id/date_from/date_to）。E2E TC-OS-04 断言核验属实：仅 `toHaveURL(/outsourcings\?keyword=/)` + 行存在（自建最新单必在 id 倒序第 1 页），未断言过滤——测试无法暴露该缺陷。MovementsView 预填范式参照（:185-191）存在，修复方向有效。

### BF-2 四页异步预填无会话序号守卫——✅ 真实

- 核验过程：逐处读取条目所列全部点位。
- 坐实：ReportsView `loadOperations`（:50-81）与 `submitReport`（:107-146，:124 直接取 `currentOp.value.id`）无守卫；PicksView `onOrderChange`（:69-89）与 `openEdit`（:131-157，连串 await）无守卫；ReturnsView（:51-69/:111-137）、FinishedInboundsView（:54-74/:116-135）同款；OutsourcingsView 主弹窗有 `sessionSeq` 守卫与关窗作废（:121-128 注释原文含「评审 F5，模式同 RoutingCanvasDialog」），但 openReceipt（:396-413）、openReturn（:470-499）、openReceipts/openReturns（:566-585）均无守卫——「主弹窗已修、同族未接线」的对照成立。OrdersView `onProductChange`（:103-114）无守卫，迟到「无启用 BOM」响应清空 B 单选择的行为路径成立。

### BF-3 下拉仅前 100 条且无服务端搜索——✅ 真实

- 核验过程：读取 RoutingCanvasDialog（:450-458 三类商品 per_page:100、:387 缓存注释原文「graph 回显的商品可能不在下拉前 100 条内」）及全部列举页面。
- 坐实：OutsourcingsView :590-593、PicksView :276-278、ReturnsView :228-236（生产中+已完成两路）、FinishedInboundsView :229-231、ReportsView :153、RoutingsView :167-172、BomsView :328-339、purchase/InboundsView :329-334、sales/OutboundsView :321-330 均为 `per_page: 100` 一次性装载（多数带「覆盖全量」假设注释）；后端 `OutsourcingController::index` `max(1, min(100, per_page))` 硬钳制确认（:76），无绕过通道。:387 注释自认第 101 个商品回显问题的证据原文一致——缓存只解决回显、新增选择不可达的论证成立。

### BF-4 工单创建成功但 orderDetail 失败 → 重复建单——✅ 真实

- 核验过程：读取 `OrdersView.vue:149-200`。
- 坐实：`createOrder` 成功（:178）→ `expandData.value = await productionApi.orderDetail(res.id)`（:182）失败即跳 :195 catch（`ElMessage.error((e as Error).message)`），:183 关窗与 :188-193 表单清空均不执行——弹窗保持打开、表单原值、saving 复原，再点保存必然二次 createOrder。:180-181 注释（bug #11 回归修复意图）原文核实，修复未覆盖 orderDetail 路径的论断成立。

### BF-5 三页 load() 无 catch——✅ 真实

- 核验过程：读取三个文件引用段。
- 坐实：CategoriesView :96-99 `tree.value = await categoryApi.tree()` 全裸；ProcessesView :88-94 try/finally 无 catch；NumberingConfigsView :127-135 同款 try/finally 无 catch——rejection 穿透、静默白屏的行为成立。

### BF-6 BomsView Promise.all 无兜底 + InboundsView 注释失实——✅ 真实

- 核验过程：读取两文件 onMounted。
- 坐实：BomsView :328-339 `await Promise.all([...])` 无 try/catch；purchase/InboundsView :327-341 外层有 try + 「下拉加载失败不阻塞主流程」注释，且注释「对齐 BomsView 写法」与 BomsView 实际无兜底相矛盾——注释失实成立。

### BF-7 委外/工序契约类型 number vs 后端 decimal 字符串——✅ 真实

- 核验过程：读取 `web/src/api/production.ts`（:37-39、:57-59、:207-209、:236-252）、后端三个模型 casts()、`api/dashboard.ts:5-11`。
- 坐实：前端 `quantity/received_qty/issued_qty/returned_qty/required_qty/qualified_qty/defective_qty/hours` 全部声明 `number`；后端 `OutsourcingOrder`（quantity decimal:2）、`OutsourcingOrderItem`（三字段 decimal:2）、`WorkOrderOperation`（三字段 decimal:2）cast 齐全——Laravel decimal:2 序列化为字符串，且 OutsourcingController::index/show 直接透传模型属性（`'quantity' => $o->quantity`）、`received_qty` 为 bcadd 字符串，JSON 实为 `"6.00"` 形态；对照组 dashboard.ts 全 string 与 RoutingService::graph 显式 `(float)` 均属实。消费点防御性 `Number()` 包裹（OutsourcingsView :262/:400/:485）核实存在。

### BF-8 BomsView 编辑弹窗缺 :close-on-click-modal——✅ 真实

- 核验过程：读取 BomsView :61（`width="800px"` 无该属性）与对照组 OutsourcingsView/PicksView/OrdersView/RoutingCanvasDialog（均显式设置）——全部命中。

### BF-9 画布弹窗固定 width="1200px"——✅ 真实

- 核验过程：RoutingCanvasDialog.vue :8 原文 `width="1200px"`，无 max-width 自适应；项目内其余弹窗 ≤900px 的对照经 grep 抽查成立。

### BF-10 委外列表数量列裸输出——✅ 真实

- 核验过程：OutsourcingsView :646-656（数量列 `prop="quantity"` 裸展示、同表已回收列 `formatThousand(row.received_qty ?? 0)`）；RoutingsView :38（基准数量裸输出）、AlertsView :25（`{{ a.quantity }}` 裸输出）——同一行两种格式与历史页同款均核实。

### Z-1 Vue Flow 浏览器兼容性——⚠️ 证据不足（维持）

- 核验过程：`web/package.json` 无 `browserslist` 字段（grep 确认）、`@vue-flow/core: ^1.48.2` 在列——条目的 L1 侧事实全部属实。
- 维持理由：兼容性本身需在目标浏览器真机实测拖拽/连线/fitView，代码快照无法裁决；缺失证据仍是「目标环境实测结果与浏览器支持矩阵文档」。

### Z-2 领料编辑回填剩余量上限的后端复核——✅ 补证坐实（后端有权威复核，无超领风险）

- 补证过程：读取 `web/src/views/production/PicksView.vue:131-157`（remaining_qty 取自实时 fromOrderPicks、:177 前端校验 ≤ remaining）与后端 `PickListController::update:197-240`、`approve:277-330`。
- 关键证据：update 在保存时**重新预取**最新 materialMap（:216）并 `validateRemaining` 复核（:217-218，bccomp `pick_qty ≤ required - issued`，即按订单累计已领权威复核）；更权威的是 `approve` 在事务内 `pmMap lockForUpdate()`（:300-305）后逐行锁行复查剩余（:322-324，超限 1513 整体回滚）——即便回填与保存之间另一张领料单已审核领料，本单审核时仍会被拦。
- 结论：条目担心的「无复核则超领风险」分支被代码事实否定——超领风险不存在，前端 remaining_qty 过期仅是体验问题（保存时被 1513 拒绝并提示），定级可按体验问题处理。

---

## 四、宪法设计项偏离清单核验（D-1~D-21、总表、无法验证区）

条款原文引用核对：将 D-1~D-21 各条目引用的条款原文与仓库根 `AGENTS.md`（当前生效版）逐条比对，**引用均准确**（仅省略 Markdown 强调符号与个别括注，语义无损）；「63 条【强制】条款」计数与 AGENTS.md 实际【强制】条目数（63）一致。

### D-1~D-21 判定与核验过程

| 编号 | 判定 | 核验过程简述 |
| --- | --- | --- |
| D-1 写操作未经 Service | ✅ | grep 复核：28 个 Api 控制器中 25 个含直接写库、13 个含 DB::transaction（文件清单与 B-102 一致）；Service 目录恰 10 个文件；PickList :135/:221/:257/:281/:381、PurchaseInbound :341-431、OperationReport :51 等抽读属实。注：条目中 OutsourcingController 行号引用（144/271/358/517/617）与实际（144/271/309/339/455/614）有漂移；「51 处」实测 53 处——均不影响成立 |
| D-2 校验未集中 FormRequest | ✅ | grep `$request->validate` 实测 **47 处**（与条目数字精确一致）；`app/Http/Requests/` 恰 3 个类（RoleRequest/UserStoreRequest/UserUpdateRequest）；PickListController :405 validatePayload 属实 |
| D-3 浮点参与数量比较累加 | ✅ | 同 B-2/B-107 证据链：InventoryService :48/:58/:91、CheckController :273/:288、19 处控制器 (float) 比较、对照 PickList :322-330 与 PurchaseInbound :381-385 bccomp——全部核实 |
| D-4 金额存储形态与前端换算 | ✅ | 迁移 :19/:34/:36 `decimal(14,2)` 且 comment 标注「（分）」自相矛盾属实；前端 purchase/OrdersView.vue :57（`Number*Number+toFixed`）、:67（`Math.round(*100)`）浮点算术属实 |
| D-5 魔法状态值与 STATUS_* 缺失 | ✅ | BomController :135（`$status === 1`）/:138（`update(['status' => 0])`）、AuthController :36（`$user->status !== 1`）原文命中；11 个模型（BomHeader/Category/Customer/DictionaryItem/Location/Process/Product/Supplier/Unit/User/Warehouse）grep STATUS_ 全部 0 命中——**精确一致** |
| D-6 索引命名无前缀 | ✅ | balance_unique（inventory :22）、movement_product_wh/:movement_source_type（:41/:42）、purchase_orders_status/:supplier（:24/:25）、document_sequences_type_date_unique（:21）逐行命中；routing 迁移匿名 index 与 `uniq_routing_nodes_node_no` 对照属实 |
| D-7 时间/布尔字段命名 | ✅ | check_time（inventory :52）、report_time（production :64）、enabled 布尔（document_number_configs :19）逐行命中 |
| D-8 部分列表未分页 | ✅ | CategoryController :20 / ProcessController :20 全量 `get()` 逐行命中；SalesOrder available 全量（同 B-106） |
| D-9 select 通配列 | ✅ | `'\w*\.\*'` 全库 grep 实测 **11 处**，与条目列举的 11 个 file:line **逐一精确命中**（InventoryController:67、Outsourcing:51、FinishedInbound:39、PickList:41、ProductionOrder:43、SalesOrder:37、SalesOutbound:46、PurchaseInbound:45、ReportService:255、PurchaseOrder:36、ReturnList:38）；条目自述「12 处」为计数小误（差 1） |
| D-10 错误路径循环内查询 | ✅ | PickListController :333 错误分支内 `Product::find(...)->code` 命中 |
| D-11 dictionaries/code 无权限 | ✅ | routes/api.php :61 原文命中（组内无 permission 中间件，注释理由一致） |
| D-12 散装 response()->json | ✅ | BomController :164/:171/:176 三处 `abort(response()->json([...], 200))` 命中 |
| D-13 领域异常不经全局 handler | ✅ | bootstrap/app.php 通读：渲染器仅 Authentication/Authorization/NotFound/Validation 四类，**无 *Exception 渲染器**；控制器自行 catch 领域异常实测 **50 处**（条目「30+ 处」成立）；PurchaseInbound :419-425 catch(Throwable) 仅 Log::warning 属实（有中文论证注释） |
| D-14 日志分级缺失 | ✅ | grep `Log::(info|warning|error|debug)` 实测**恰 5 处且全部 warning**（PurchaseInbound:424、ProductionOrder:121/324、DocumentSequenceService:61/73）——与条目**逐字一致**，info/error 零覆盖属实 |
| D-15 硬编码色值 | ✅ | 复现计数：.vue 文件 149 处 + main.css 19 处 = **168 处**（除 tokens.css）——与条目数字**精确一致**；分布 38 个 .vue 文件 ≥「20+ 文件」 |
| D-16 页面自实现格式化 | ✅ | grep toFixed/toLocaleString 实测 **18 处、9 个文件**——与条目**精确一致**；sales/OutboundsView:75、sales/OrdersView:58 抽查命中 |
| D-17 表单未用 el-form rules | ✅ | 实测含 `<el-form` 视图 **23 个**、含 `:rules` 的 **2 个**（LoginView、NumberingConfigsView）——与条目**精确一致** |
| D-18 聚合未下推 SQL | ✅ | DashboardService :149（全表 `select('product_id','quantity')->get()`）与 :158-166（PHP foreach bcmath 累加）命中；FinishedInboundController :74 `$f->items->sum('quantity')` 命中 |
| D-19 种子口令与 token 存储 | ✅ | RbacSeeder :155 `config('app.admin_password') ?: 'admin123'` **逐字命中**；auth.ts localStorage token 属实（setItem 实际在 :19，条目写 :14，函数始于 :14——行号小漂移） |
| D-20 服务级 Unit 测试缺失 | ✅ | tests/Unit/ 恰 2 文件（ExampleTest、RoutingServiceTest）——精确一致 |
| D-21 残留进程清理未落地 | ✅ | playwright.config.ts 两处 `reuseExistingServer: !process.env.CI`（实际 :67/:78，条目写 :64/:75——行号小漂移，事实一致）；package.json scripts 无任何清理脚本（仅 test:e2e）确认 |

### 63 条款对照总表抽检

- 总表结论与明细区 D 编号的映射（#9→D-1、#10→D-2、#23→D-3、#47→D-14 等）核对一致；抽检 #27（§4.3.1 符合，PurchaseInbound :341-415 先锁后校验后变更）、#28（§4.3.2 符合，锁序注释 PickList :287-288、SalesOutbound :341 原文存在）、#59（ci.yml 门禁）、#62（PR #31-#33 合并记录，git log 可见 `b03b989 (#32)`、`d7d3acb (#33)`、`ba4a211 Merge PR #31`）均属实。
- **发现一处统计行数字错误（不影响任何条目判定）**：总表末尾统计行「符合 38 / 部分偏离 17 / 完全偏离 3 / 无法验证 6」与其下方修正注（完全偏离 4 条）及逐行结论不符——按表格逐行实数为**符合 33 / 部分偏离 20 / 完全偏离 4 / 无法验证 6**（合计 63）。建议主控在 G3 汇总时以逐行口径为准。
- **范围口径观察（供 G3 去重参考）**：总表 #28 判 §4.3.2「符合」的证据域是「单据头→工单→明细」锁序方向；S1 B-3 判「违反」的证据域是**库存余额行**的锁序（三种风格并存）。两者各自属实、不构成矛盾，但 #28 的「符合」结论未覆盖余额行维度，B-3 的发现更全面——合并时以 B-3 为准。

### 无法验证区（6 条）归类确认

逐条复核其「无法验证」理由与 L1 证据：①② 主观评审条款（§1.1/§1.2）、③ §1.4 过程性条款——归类合理；④ §9.2 覆盖率——`ci.yml :28/:102` 确为 `coverage: none`、phpunit.xml 无覆盖率配置，证据属实，归类合理；⑤ §9.4 历史时序——需全量历史比对，归类合理；⑥ §10.4 main 保护设置存于 GitHub 服务端——本地不可见，归类合理（`gh api` 需权限，本次未调用）；⑦ §10.5 vendor 未手改——归类合理。**6 条归类全部合理**，无需改判。

---

## 五、统计汇总

| 清单 | 判定条目 | ✅ 真实 | ❌ 伪 | ⚠️ 证据不足 |
| --- | --- | --- | --- | --- |
| 后端 S1（B-1~B-5） | 5 | 5（B-4/B-5 为补证坐实） | 0 | 0 |
| 后端 S2（B-101~B-108） | 8 | 8（B-108 为静态事实坐实+注记） | 0 | 0 |
| 前端 S3（BF-1~BF-10、Z-1/Z-2） | 12 | 11（Z-2 为补证坐实） | 0 | 1（Z-1） |
| 宪法 S4（D-1~D-21） | 21 | 21 | 0 | 0 |
| **合计** | **46** | **45** | **0** | **1** |

无法验证区 6 条：归类合理性已确认（见第四节），未逐条判定（按任务约定）。

交叉引用关系核实（供 G3 去重）：
- B-2 ≡ D-3 引擎部分、B-107 ≡ D-3 散布部分（同一代码事实，S1/S2/S4 三方收录互补不重复，声明属实）；
- B-108 是 B-3 的盘点子案（属实）；
- B-102 ≡ D-1（同一偏离，行号/计数以本报告勘误为准）；
- B-103 在 S1 附带发现区与 S2 正式区双收录（S1 已自声明，属实）。

---

## 六、核验附带发现（清单外新问题，仅记录）

1. **InventoryController:234-238**（B-107 族a 未收录点位）：`(float) $r->safety_min > 0 && $qty < (float) $r->safety_min` 等三处浮点比较用于安全上下限预警判定——与 B-107 族 a 同族，扫描清单未收录（影响同样为铁律字面冲突、实际值域无误差）。
2. **B-1 的 URL 直达旁路**（B-5 核验过程中发现）：`/production/outsourcings?order_id=<任意工单id>` 会触发 `openCreate(orderId)` 无条件预填（OutsourcingsView :598-601 → :226-243），`orderDetail` 无状态过滤——手工构造 URL 可对草稿工单预填建单弹窗（等价于 API 直调的 UI 通道），修复 B-1 时可顺带在 onOrderChange 内按状态拒绝预填。
3. **S4 总表统计行数字错误**（详见第四节）：符合 38/部分偏离 17/完全偏离 3 应为 33/20/4；修正注自相矛盾处已在报告指出，属清单文件笔误，非代码问题。
4. 行号/计数小误汇总（均不推翻条目）：B-102 与 D-1 的 ProductController :101→实际 :103、「51 处」→实测 53 处、D-1 的 Outsourcing 三处行号漂移、D-9 的「12 处」→实测 11 处、D-19 的 auth.ts:14→实际 :19、D-21 的 :64/:75→实际 :67/:78、B-108 的「多数审核路径 attempts=2」表述（实况为建单路径普遍 attempts=2、审核路径仅委外 approve）。

---

## 七、只读纪律自检

- 未修改任何 `server/`、`web/` 代码文件；未运行任何写库/写文件命令；未执行 git 写操作（无 add/commit/push/stash）。
- `docs/` 整目录在 `.gitignore` 第 13 行被忽略（内部开发文档本地保留），故本次任务的全部产物（三份清单修改 + 本报告新建）均不进入 git 状态；实际 `git status --short` 输出如下，其中 `AGENTS.md.draft / CHANGELOG.md / TASK.md` 为**核验开始前即存在的无关未跟踪文件**（S4 审计亦引用过 AGENTS.md.draft 的存在），与本次任务无关：

```
?? AGENTS.md.draft
?? CHANGELOG.md
?? TASK.md
```

- 实际变更清单（均在被忽略的 docs/ 内，属任务预期产物）：三份清单文件追加核验标注并统一为 LF 行尾（原文件为 CRLF，本次顺带按宪法 §1.6 编码基线归一）；新建本报告（UTF-8 无 BOM、LF、末尾单一换行）。

---

*核验执行：清单核验专员 V1（只读）；判定依据均为可复核的代码行/git 证据。*
