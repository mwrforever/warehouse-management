# 进度文档（2026-08-13 安全扫描问题修复波次）

> 本文件登记 2026-08-13 依据 `docs/bugs/2026-08-13-问题汇总.md`（B01-B11）执行的跨模块安全/健壮性修复波次，供后续会话跟踪。
> 上一会话旧进度：`docs/progress/2026-08-13-生产管理模块实施.md`（生产模块，PR #7 已合入 main，commit 8e0b098）。

## 元信息 (Meta)

- workspace_dir: D:\code\project\php-design
- 任务一句话描述: 全仓 24h 变更安全扫描问题清单（B01-B11）逐项核实真伪 → 修复真实问题 → 标注不存在项
- 版本控制: **修复代码未提交**（23 个文件 + docs/bugs/ 更新在工作区）；合入需走 feat 分支 + PR 流程

## 1. 结论速览（逐项详情见 docs/bugs/2026-08-13-问题汇总.md「修复波次结论」表）

- **已修复（9 项）**：
  - B01 admin 提权（高）：UserController 1003 保护族——非管理员不可改/删/重置管理员账号（内置 admin 或挂 admin 角色者），不可授予 admin 角色（update/store 均拦）；+7 测试
  - B02 角色保留码（中高）：核实「直接创建第二个 admin」被唯一索引拦（不成立），真实路径是 admin 角色 code 改出后重建——RoleRequest 内置角色 code 锁定 `in:admin`、其他角色 `not_in:admin`；+4 测试
  - B03 角色编辑回填竞态（中高）：RolesView 权限树加载 Promise 缓存 + openEdit 回填前 await，失败阻断弹窗防空勾选清空权限
  - B04 库存种子非幂等（中高）：InventorySeeder 入账前查余额行，已存在即跳过；+1 幂等测试
  - B05 商品删除守卫漏 4 表（中高）：ProductController 补 inventory_check_items/pick_list_items/return_list_items/finished_inbound_items；+4 测试
  - B08 仓库/库位删除守卫缺口（中）：补 inventory_movements/inventory_checks/inventory_check_items 与领退料/委外/成品入库单据表（仓库 10 表、库位 10 表）；+4 测试
  - B09 CSV 全量内存（中）：exportBalances 改 `cursor()` 游标 + `fputcsv` 真流式（保留 BOM 与无引号表头，导出测试锁定格式）
  - B10 供应商漏委外单（低→可达）：补 outsourcing_orders 守卫（委外控制器已上线，原「不可达」判定过期）；+1 测试
  - B11 低优先级 4 项：扫码原料禁售前端过滤（Orders/Outbounds 两处）/ 5 处弹窗裸 await 补 try-catch / vite 代理 localhost→127.0.0.1 / RbacSeeder 支持 `ADMIN_PASSWORD` 环境变量（默认 admin123 不变，E2E 不受影响）
- **标注不修复（2 项）**：
  - B06 报工开工锁序死锁：**此前已修复**（生产模块修复波次已统一 op→order 全局锁序，审查基于过时版本）——仅复核标注
  - B07 盘点盘盈无余额行：**判定不存在（一致性设计）**——无余额商品在 store/update 即被 1205 拦截（消息准确），前端同拦截，测试锁定；approve 的 1206 路径不可达。账外盘盈属功能需求，**用户裁决：暂不做**（实现改动点清单见 B07 文档「处理结论」）

## 2. 主要改动文件

- 后端：`UserController` / `RoleRequest` / `InventorySeeder` / `ProductController` / `WarehouseController` / `SupplierController` / `InventoryController` / `RbacSeeder`
- 前端：`RolesView` / `OrdersView` / `OutboundsView` / `DictionariesView` / `WarehousesView` / `BomsView` / `UsersView` / `ProductsView` / `vite.config.ts`
- 测试：UserManagementTest +7 / RoleManagementTest +4 / ProductTest +4 / WarehouseTest +4 / SupplierTest +1 / InventoryStructureTest +1（幂等）

## 3. 验证结果（全绿）

- 后端：phpunit **375 passed（1484 断言）**；phpstan level 5 **0 错误**；pint 139 文件 PASS；phpcs 改动文件 0 错误
- 前端：vue-tsc 0 错误；eslint 0；prettier 通过；vitest **61 passed**

## 4. 待办 (Pending)

- [ ] 修复代码提交（按 git 合入流程：feat 分支 → pre-push 门禁 → PR → 云端 CI 全绿 → merge）
- [ ] B07 账外盘盈：用户已裁决暂不做；若后续要做，改动点已在 B07 文档列明（store/update 无余额+actual>0 放行 book=0、approve 按账面 0 比对、前端 book 显示 0、1205 用例口径调整）
- [ ] 旧进度文档 stale 条目已同步标记（见 `2026-08-13-生产管理模块实施.md` 第 5 节）
