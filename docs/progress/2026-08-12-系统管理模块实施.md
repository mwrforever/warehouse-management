# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\php-design
- saved_at: 2026-08-12T15:47:53+08:00
- 版本控制提交号: 2502f6ab30491daf8b742d8dfb5847529b04040f（分支 main，远程 git@github.com:mwrforever/warehouse-management.git 已同步）
- 任务一句话描述: 进销存+生产管理系统（Nexus Factory）——拆分 8 模块细化 spec 与 E2E 测试文档，并按 superpowers SDD 流程实施，当前已完成系统管理模块

## 1. 已完成 (Completed) — 简要标记
- [x] 主 spec 按 8 大模块拆分为 8 份细化 spec（`docs/superpowers/specs/2026-08-12-{模块}-spec.md`）+ 8 份 E2E 测试文档（`docs/test/2026-08-12-{模块}端到端测试.md`），交叉引用与依赖链完整
- [x] 环境脚手架：Docker MySQL 8.4（`docker-compose.yml`）、Laravel 13.25（`server/`）、Vue 3+TS+Vite（`web/`）、设计令牌 `web/src/styles/tokens.css`
- [x] 系统管理模块完整实施并推送远程（17 commits）：认证（Sanctum login/logout/me）、RBAC（EnsurePermission 中间件 + 种子 admin/admin123 + operator）、用户管理（CRUD/角色分配/重置密码/token 撤销）、角色权限（权限分组接口/1004/1007 保护）、字典（CRUD/按编码取值）、前端基座（axios 封装/路由守卫/深色布局/登录页）+ 用户/角色/字典三页面（按钮按权限显隐）
- [x] 测试全绿：后端 PHPUnit 48 passed（117 断言）、前端 Vitest 14 passed、E2E 12/12 通过（playwright-cli 执行 TC-SYS-01~12，结果记录在 E2E 文档 §5）
- [x] SDD 实施计划：`docs/superpowers/plans/2026-08-12-system-management.md`（9 Task + 最终审查修复，含 TDD 测试代码，可直接复用于下一模块）
- [x] git 仓库初始化（main）+ .gitattributes LF 强制 + 远程关联推送

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 基础资料模块实施（下一目标）
- 目标: 按 `docs/superpowers/specs/2026-08-12-master-data-spec.md` 实现商品/分类/单位/仓库库位/供应商/客户/工序/BOM 的后端 API 与前端页面，通过 PHPUnit/Vitest 全绿 + E2E TC-MST-01~11（`docs/test/2026-08-12-基础资料模块端到端测试.md`）全过
- 当前状态: 细化 spec 与 E2E 文档已就绪；系统管理模块（认证/RBAC/字典/用户）已全部可复用；`server/` 与 `web/` 工程可运行；实施计划**未创建**
- 涉及文件/产物:
  - `docs/superpowers/specs/2026-08-12-master-data-spec.md` [修改] — 细化设计（10 张表、API 契约、错误码 1101-1124）
  - `docs/test/2026-08-12-基础资料模块端到端测试.md` [修改] — 11 个用例验收标准
  - `docs/superpowers/plans/2026-08-12-master-data.md` [待创建] — 实施计划（参照系统管理模块计划格式）
  - `server/app/Models/{Product,Category,Unit,Warehouse,Location,Supplier,Customer,Process,BomHeader,BomItem}.php` [待创建]
  - `server/app/Http/Controllers/Api/*` [待创建] + `server/routes/api.php` [修改]
  - `server/database/migrations/*` [待创建] + `RbacSeeder` 或新 Seeder 补权限种子 [修改]
  - `web/src/views/master/*.vue` [待创建]（8 个页面）+ `web/src/api/*.ts` [待创建] + `web/src/router/index.ts` [修改]
- 已锁定决策:
  - 权限 code 沿用 `{资源}.{动作}`：`product.*`/`category.*`/`unit.*`/`warehouse.*`/`supplier.*`/`customer.*`/`process.*`/`bom.*`；新权限需追加到 RbacSeeder 并同步 admin 角色
  - 错误码段 1101-1124（见 spec §4）；统一响应 `{code,message,data}`；金额一律分单位存储（本模块无金额）
  - 商品类型枚举 raw_material/semi_finished/finished；BOM 头关联成品、明细仅原料/半成品；同成品启用版本唯一
  - 引用保护：被业务单据引用的主数据删除一律拒绝（1102/1104/1106/1109/1111/1113/1116/1121）
  - 扫码交互：条码输入框自动聚焦，扫枪回车即时校验（`GET /api/v1/products/barcode/{barcode}`）
- 阻塞点/风险:
  - 表结构设计需与主 spec §6 数据模型一致（products 含 safety_min/safety_max 供库存预警）
  - 分类两级限制（1124）、BOM 明细重复物料（1123）、min>max（1122）等边界码需在 FormRequest/Service 层实现
- **下一步具体动作**:
  1. 用 superpowers:writing-plans 生成实施计划：`docs/superpowers/plans/2026-08-12-master-data.md`（参照系统管理模块计划——先迁移+模型+Seeder，再按资源分组做 API+TDD，最后前端页面+Vitest+E2E）
  2. 用 superpowers:subagent-driven-development 执行计划（每 Task 派发 implementer + reviewer；注意先补 `RbacSeeder` 权限种子再开发接口，避免中间件 403）
- 验证方式: `php artisan test` 与 `npx vitest run` 全绿；E2E TC-MST-01~11 全过（playwright-cli，dev server :8000/:5173）

### 2.2 库存管理模块实施（基础资料之后）
- 目标: 实现 `docs/superpowers/specs/2026-08-12-inventory-spec.md`——库存余额/流水引擎（InventoryService）、盘点单全链路、预警；E2E TC-INV-01~12 全过
- 当前状态: 细化 spec 与 E2E 文档就绪；未开始
- 涉及文件/产物:
  - `docs/superpowers/specs/2026-08-12-inventory-spec.md` [修改] — 核心不变式（流水唯一事实来源、事务双写、审核幂等）
  - `docs/test/2026-08-12-库存管理模块端到端测试.md` [修改]
  - `server/app/Services/InventoryService.php` [待创建] — 全系统唯一库存变动入口（后续采购/销售/生产模块均调用）
- 已锁定决策: 余额=流水求和恒等式；变动一律事务内写 `inventory_movements` + 更新 `inventory_balances`；盘点审核行锁防并发；预警查询时计算不落库
- 阻塞点/风险: 本模块是核心不变式载体——**主 spec §10 要求核心功能 100% 单测覆盖**；InventoryService 接口签名需在计划中精确定义（供采购/销售/生产模块复用）
- **下一步具体动作**: 在基础资料模块完成后，按主 spec §11 迭代 3 实施；计划生成方式同 2.1
- 验证方式: 恒等式测试（INV-04）+ 盘盈盘亏（INV-08/09）+ 审核幂等（INV-10）全过

### 2.3 采购/销售/生产/报表/仪表盘模块（依次）
- 目标: 按主 spec §11 迭代 4-6 依次实施（采购→销售→生产→报表→仪表盘），每模块走同一 SDD 流程
- 当前状态: 细化 spec + E2E 文档全部就绪（`docs/superpowers/specs/` 与 `docs/test/`）；未开始
- 已锁定决策: 单据「制单→审核」两级状态（审核后库存才变动）；状态机见各 spec §6；单号规则 `{模块前缀}{yyyyMMdd}-{流水}`；生产工单 BOM 展开、工序报工自动流转、委外发出/回收、成品入库联动工单完成
- **下一步具体动作**: 按顺序逐模块生成计划+执行（依赖链：基础资料→库存→采购/销售→生产→报表→仪表盘）
- 验证方式: 各模块 E2E 文档 TC 全过；跨模块联动（流水单号跳转、预警三级联动、报表口径一致）按各文档 §4 判定

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本:
  - PHP 8.5.9 NTS x64: `D:\code\envs\php\8.5.9\php.exe`（已入用户 PATH，新终端直接 `php`）
  - Composer 2.10.2: `D:\code\envs\composer\2.10.2\composer.phar`（已入 PATH；**全局镜像为阿里云且不同步 Laravel 13，装包必须官方源**——用全新 COMPOSER_HOME 绕过：`COMPOSER_HOME=/c/Users/Lenovo/AppData/Local/Temp/composer-official php .../composer.phar require ...`）
  - Laravel 13.25.0（server/，Sanctum 4.3.3）、MySQL 8.4（Docker 容器 php-design-mysql，db=php_design/user=php/pass=secret/port=3306）、Node/npm（web/，Vue 3.5 + Vite + Element Plus + Pinia + Vue Router + axios，Vitest 已配 jsdom）
- 关键配置:
  - `server/.env`（gitignore 不入库）: DB 已配 MySQL php_design；APP_URL=http://localhost:8000；APP_KEY 已生成
  - `server/.env.example` [已修复] — DB 块为 mysql 配置（新 clone 直接可用）
  - `web/vite.config.ts`: 代理 `/api` → `http://localhost:8000`；端口 5173
- 安装/构建/启动命令:
  ```bash
  cd D:/code/project/php-design
  docker compose up -d                                   # MySQL 8.4
  cd server && php artisan migrate --seed && php artisan serve   # 后端 :8000
  cd ../web && npm run dev                               # 前端 :5173
  cd server && php artisan test && cd ../web && npx vitest run    # 全量测试
  ```

## 4. 全局约定 (Locked Conventions)
- **目录结构**: `server/`（Laravel 13 API，`/api/v1/*`，routes/api.php）；`web/`（Vue 3 前端）；`docs/superpowers/specs|plans/`（spec 与实施计划）；`docs/test/`（E2E 测试文档，命名 `{yyyy-mm-dd}-{标题}.md`）；`docs/progress/`（本进度文档）；`design-system/nexus-factory/MASTER.md`（设计系统）
- **SDD 实施流程（不可跳过）**: 每模块 = writing-plans 生成计划（`docs/superpowers/plans/`）→ subagent-driven-development 逐 Task 执行（TDD、每 Task reviewer、ledger 在 `.superpowers/sdd/`，注意 workspace 用完即删）→ E2E 文档全过 → 推送远程
- **接口契约**: 统一响应 `{code,message,data}`（code=0 成功；401/403/404/422 均 JSON 信封；422 仅 username Unique 映射 1002，其余 422）；错误码 1001-1008 系统管理、1101-1124 基础资料、1201-1206 库存、1301-1312 采购、1401-1412 销售、1501-1528 生产、1601 报表（见各 spec §4）；权限中间件 `permission:{资源}.{动作}`；分页 `{items,total,page,per_page}`；per_page 钳制 1-100；密码规则 min:8 含字母数字
- **RBAC**: 权限只授权给角色；新模块权限追加 RbacSeeder 并 sync 到 admin 角色；前端按钮用 `auth.has()` 门控，权限码必须与后端路由中间件一致（Task 9 曾因字典项按钮码错位返工）
- **用户硬性约束（AGENTS.md）**: 全部注释/日志/文档中文；UTF-8 无 BOM；LF 行尾（.gitattributes 已强制）；无死代码；核心路径（认证/权限/库存一致性/金额）100% 单测；E2E 未过不提交合并
- **设计系统**: 前端样式以 nexus-factory 为准（主色 #334155、强调绿 #059669、背景 #F8FAFC、Fira Code+Fira Sans），不得退回 Element Plus 默认蓝白
- **单号规则**: `{模块前缀}{yyyyMMdd}-{流水}`（PO/PI/SO/SOUT/MO/PL/RL/OS/OSR/FI/CK 等）
- **git**: 提交前确认 `git config user.name`（已设 dev）；完成后 `git push origin main`（远程 git@github.com:mwrforever/warehouse-management.git）
