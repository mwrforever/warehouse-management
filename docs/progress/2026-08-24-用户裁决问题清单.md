# 2026-08-24 用户裁决问题清单（一次性汇总）

> 依据工作流文档总则 2（L3）与 G3 门禁要求：⚠️ 证据不足项、模糊优化项、超出「最小改动」边界的修复，一次性汇总用户裁决，禁止分批打扰。每项附背景、可选项、建议方案。
> 裁决日期：2026-08-25（待用户填写结论）

---

## R1. 宪法架构治理类偏离的本次处理范围（D-1 / D-2 / D-13 / D-14·info 部分 / D-15 / D-17）

**背景**：审计确认 4 项完全偏离（D-1 写操作未经 Service：25/28 控制器直接写库、51 处 DB::transaction 在控制器；D-2 校验未集中 FormRequest：47 处内联 validate；D-3 库存引擎浮点【已列入确定性修复清单，不占裁决】；D-14 日志：全后端仅 5 处全 warn）与大型部分偏离（D-13 异常不经全局 handler、30+ 控制器自行 catch；D-15 前端 168 处硬编码色值；D-17 23 个表单视图仅 2 个用 :rules）。D-1/D-2/D-13 的完整修复=按单据域把控制器事务/校验/异常处理下沉 Service，属架构级重构，一次修复波无法完成，且工作流范围外条款（1.2.1）排除架构重构。

**可选项**：
- **A（建议）最小落地子集 + 分期计划**：本次仅修 D-13 的全局异常渲染器注册（bootstrap renderable，小改动）+ D-14 的全局 handler error 日志（含堆栈+单号）+ D-15 的核心文件收编（MainLayout/LoginView/DashboardView/OperationGraph 主题色入 tokens.css）+ stylelint `color-no-hex` 规则防增量 + D-17 优先补采购/销售/生产单据表单一批 rules；D-1/D-2/D-13 剩余部分（控制器下沉+FormRequest 化+删 catch）与 D-14 info 日志、D-15/D-17 剩余量登记 TASK.md 分期计划，本次不动代码。
- **B 全量纳入本次**：含 D-1/D-2 完整下沉（工期数倍，回归面全域，不建议）。
- **C 全部豁免备案**：本次不动任何架构治理项，仅登记 TASK.md。

**建议**：A。

## R2. 金额存储口径（D-4：§4.1.1「分单位整数」vs §6.2「decimal 或分单位整数」条款内部张力）

**背景**：后端 bcmath 计算链路已合规；金额列为 decimal(14,2)（注释却标「分」），前端行金额用 `Number(qty)*Number(price)` 浮点乘法（purchase/OrdersView.vue:57/67/135/174、sales/OrdersView.vue:58、sales/OutboundsView.vue:75）。S4 附带发现指出两条款互相矛盾，需定口径。

**可选项**：
- **A（建议）维持 decimal(14,2) + 备案豁免 + 前端整数化运算**：口径定「元两位小数 decimal 存储」，宪法豁免在 PR 说明与 TASK.md 备案（宪法修订走评审不在本次范围）；前端 6 处浮点乘法改为「分单位整数/字符串运算 + formatYuan 展示」（本次修，并入 D-3 修复波前后端各一提交）。
- **B 金额列迁移分单位整数**：全库金额列 decimal→bigint 迁移 + 全链路改造（大迁移，回归面全域，不建议本次）。
- **C 完全维持现状**：前端浮点乘法也不改（保留已知精度风险观感问题）。

**建议**：A。

## R3. 委外回收/退回流水单号回补方案（P-2 与 PF-2 同源问题、方案二选一）

**背景**：OutsourcingController 回收/退回是全项目唯二「先 apply 后取号」的例外（:491-525/:654-683），流水单号靠事后 UPDATE 回补，只能走 `movement_source_type` 单列索引范围扫描，流水表随业务量确定性退化，且并发事务间存在 movements 行锁等待窗口。两个扫描 agent 给出不同方案（V2 均已核验真实）。

**可选项**：
- **A（建议）PF-2 语句重排**：先取号建单再 apply，彻底删除两处回补 UPDATE，向全项目统一惯例收敛；V2 核验确认锁序收敛、无新 ABBA 死锁方向。改动中等（回收/退回两条事务重排），测试面两块。
- **B P-2 方案 a**：`InventoryService::apply` 返回 movement id，主键点查回补（改动最小，但保留「先 apply 后取号」例外模式与两次写）。
- **C 加 (source_type, source_id) 复合索引**：只解决扫描退化，不解决锁等待窗口（最保守）。

**建议**：A。

## R4. 杂项按建议默认处理（打包裁决）

以下各项均已在清单中附完整背景，建议按默认处理；如需逐项再议请在答复中说明。

| # | 项 | 背景 | 建议默认处理 |
| --- | --- | --- | --- |
| R4-1 | **docs/ 提交冲突**（决策日志 #1） | 工作流文档要求提交 5 份清单+进度文件，但 .gitignore 自 d6e7b12 起忽略 docs/（「内部开发文档，不上传 GitHub」） | **维持 .gitignore 不提交**；DoD「产物已提交」条目按「已落盘 + 裁决记录」关闭 |
| R4-2 | **D-7 字段更名**（check_time/report_time→xxx_at、enabled→is_enabled） | 存量迁移更名，无功能影响，涉及迁移评审 | 豁免备案登记 TASK.md，本次不修 |
| R4-3 | **D-19 token localStorage** | token 型 SPA 常规做法，字面违反 §8.5 | 豁免备案（XSS 权衡说明）；种子口令强制环境注入部分列入确定性修复 |
| R4-4 | **Z-1 Vue Flow 浏览器兼容** | 需真机实测，无法从仓库事实裁决 | 定目标浏览器基线（如 Chrome/Edge ≥ 100）写入 PR 说明，不做兼容代码 |
| R4-5 | **B-4 BOM 非整除折算截断** | bcdiv 截断舍入无 spec 明文，是否属设计意图待产品定 | 按现状（截断）在 PR 说明标注行为备案，不改代码 |
| R4-6 | **性能待裁决 3 项**（P1-D-1 路线逐行 INSERT / P1-D-2 委外节点预载粒度 / PJ-1 画布开窗 4 请求） | 量级极小，两位扫描员均认为维持合理 | 全部维持不修 |
| R4-7 | **D-8 主数据全量列表豁免**（分类/工序/字典项树与下拉） | 有界主数据全量返回可辩护，条款无豁免 | available 类并入 BF-3/B-106 修复（服务端搜索+上限）；树/字典豁免备案 TASK.md |

**可选项**：
- **A（建议）按上表默认处理全部杂项**
- **B 杂项全修**（D-7 迁移更名 + token 迁 Sanctum SPA + Z-1 兼容降级 + B-4 改舍入策略 + 性能 3 项实施 + docs 强制提交——工期大增）
- **C 部分再议**（在答复中指出要改的子项编号）

**建议**：A。

---

## 已确定无需裁决、直接进入修复清单的条目（供参考，详见进度文件修复清单）

B-1、B-3（含 B-108）、B-101、B-103、B-104/P-1/PF-1、B-105、BF-1、BF-2、BF-3/B-106（含 D-8·available 部分）、BF-4、BF-5~BF-10、D-3/B-2/B-107（含 V1 附带发现 InventoryController 3 处）、D-5、D-9、D-10、D-11、D-12、D-14·error 部分、D-16、D-18、D-19·种子口令部分、D-20、D-21、P-3、P-4、PF-3、R2-A 前端金额运算部分（若 R2 选 A）。

## 裁决结论（用户填写）

- R1：**B 全量纳入本次**（2026-08-25 AskUserQuestion 选定）——D-1（写操作下沉 Service）、D-2（FormRequest 化）、D-13（异常走全局 handler）、D-14（info 日志补全）、D-15（硬编码色值收编 + stylelint 防增量）、D-17（表单 rules 补齐）全部本次实施，含控制器事务/校验/异常处理下沉（架构级重构，工期数倍、回归面全域，按裁决执行）
- R2：**自定义方案（B 变体 + 前端展示升级）**——用户原话：「后端全部设计成分单位，前端展示上需要处理单位升级，展示元以及分的小数位」。落地口径：① 后端全库金额列 decimal(14,2) → bigint 分单位整数迁移（含数据转换与迁移评审）；② 前端展示层单位升级：分 → 元（两位小数），统一经 formatYuan，输入路径元 → 分转换；③ 前端 6 处浮点乘法一并消除（改为分单位整数/字符串运算）
- R3：**A PF-2 语句重排**——先取号建单再 apply，删除两处回补 UPDATE，向全项目统一惯例收敛（V2 已核验锁序收敛、无新 ABBA 死锁方向）
- R4：**B 杂项全修**（2026-08-25 AskUserQuestion 选定）——R4-1 docs/ 强制提交（git add -f，解除 .gitignore 忽略，DoD「产物已提交」按正常关闭）；R4-2 D-7 字段更名迁移（check_time/report_time→xxx_at、enabled→is_enabled）；R4-3 D-19 token 迁 Sanctum SPA 模式；R4-4 Z-1 Vue Flow 浏览器兼容降级实现（定目标浏览器基线）；R4-5 B-4 BOM 折算改舍入策略（先定规则再改 bcdiv 调用点）；R4-6 性能 3 项实施（P1-D-1 路线逐行 INSERT / P1-D-2 委外节点预载粒度 / PJ-1 画布开窗 4 请求）；R4-7 D-8 available 部分并入 BF-3/B-106（不变）
