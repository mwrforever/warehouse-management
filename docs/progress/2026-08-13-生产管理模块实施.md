# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\php-design
- saved_at: 2026-08-13T17:00:00+08:00
- 版本控制提交号: 627e129（feat/production 分支头，21 commits `56d33a7..627e129`；待 PR 合入 main）
- 任务一句话描述: 进销存+生产管理系统（Nexus Factory）——按 superpowers SDD 流程实施**生产管理模块**（生产工单 BOM 展开→下达/开工→领退料→工序报工自动流转→委外收发→成品入库满产自动完成工单，六单据审核事务统一经 InventoryService）已完成全绿；下一目标为统计报表模块（spec 已就绪）

## 1. 已完成 (Completed) — 简要标记
- [x] 生产管理模块完整实施（21 commits `56d33a7..627e129`，SDD 流程 13 Task 逐 Task 审查 + 4 轮修复循环（Task 5 nullable 500 / Task 8 锁序 ABBA 三连修）+ 最终双视角全分支审查 + 1 轮修复波次；PHPUnit 353（1441 断言）、E2E 54/54、phpstan 0、前端门禁全绿；待 PR 合入 main）：
  - 后端：12 表迁移（工单域 4 + 单据域 8；**补充 production_order_materials 物料需求快照表**——spec 模型清单遗漏但 §8 快照语义必需）+ 12 模型 + 24 项权限（6 资源×4 动作，group=生产管理）+ **DocumentSequence 追加 TYPE_MO/PL/RL/OS/OSR/FI 六常量（零迁移）** + ProductController 1116 链补 production_order_materials.material_id 接线 + 1113/1121/1116 删除保护补测
  - **ProductionOrderController**（草稿 CRUD **事务内锁行复查 1503/1504** + 单据引用删除保护 1504 + store 事务内锁成品行取启用 BOM **展开快照物料需求与工序序列**（1501/1502）+ release 缺料警告（Σ 全仓余额只读，warnings 不阻断 1505）+ start 开工（1506）+ complete 双前置（1507/1508）+ close（复用 1505）+ materials 接口）+ ProductionOrderService（expandBom：`bcmul(bcdiv(qty, bom.quantity, 4), item.quantity, 2)` 基准产出展开 + progress）+ ProductionException
  - **OperationReportController**（报工**累计语义防并发虚报**：1510 合格累计+本次>计划、1511 合格累计+本次不良>计划——Task 5 审查裁决语义 C，brief 原 A 语义结构性死锁；1509 仅进行中可报、1512 工时负业务码、合格/不良负 422；**流转：累计合格≥计划 → 本工序完成+下一工序自动进行中**；nullable 字段漏传归一化 0 防 500（E2E 文档化载荷锁定）+ 报工记录列表）
  - **PickListController**（from-order 预填剩余量 + 草稿期 1513 校验 + **审核事务：锁单幂等 1516 → 锁物料需求行复核 1513 → 锁余额行校验 1515（消息含商品**编码**，E2E 断言 `商品[MAT-001]库存不足`）→ apply(pick,-1) → 回写 issued_qty** + issue 发料 V1 一次置全部发料）
  - **ReturnListController**（草稿期 1517 校验 + 审核事务：锁单幂等 1519 → 锁物料需求行复核 1517 → apply(return,+1) → **冲销 issued_qty**）
  - **OutsourcingController**（**委外商品=工单成品**（spec 无 product_id，E2E 锁定）——发出审核：锁单幂等 1523 → 锁工单行（状态校验）→ 锁余额行 1522 → apply(outsourcing_out,-qty)；回收 storeReceipt：锁委外单（仅草稿 422，已回收放行到 1524）→ 锁工序行 → 锁工单行 → apply(outsourcing_in,+qty) → **创建 OSR 单（创建即审核）+ 流水 source_no 回补** → 累计≥委外量 → 委外单已回收+**工序标记完成**（仅未完成工序））
  - **FinishedInboundController**（草稿期 1525/1526 校验 + 审核事务：锁单幂等 1528 → 锁工单行复核剩余产量 1525 → apply(finished_inbound,+qty) → completed_qty 累计 → **末工序已完成（一致性读）且满产 → 工单自动已完成**；$lastDone 死代码锁已删防 ABBA）
  - **锁序全局约定（Task 8 三连修复闭环，后续模块必须遵守）**：op/order 全系统同序——报工 op→next-op→order、回收 outsourcing→op→order、开工 op(seq1)→order、发出 outsourcing→order、完工/下达/关闭 order only；无任何 order→op 反向路径
  - 前端：6 页（`OrdersView` 五态标签+BOM 展开确认弹窗+下达缺料琥珀警告条+详情 tabs；`ReportsView` 工序步骤条+进行中工序报工卡片；`PicksView`/`ReturnsView` 从工单生成双入口+on-blur 上限校验+审核/发料 confirm；`OutsourcingsView` 委外工序下拉+回收弹窗「剩余可回收」；`FinishedInboundsView` 自动带成品行默认剩余产量）+ `productionApi` 40 方法 + 6 路由 + 「生产管理」菜单组 + 设计系统页覆盖 `design-system/nexus-factory/pages/production.md`（ui-ux-pro-max 查询驱动，tag-done 深绿/次级弹窗 900px 豁免注明）
  - E2E：`web/e2e/production.spec.ts` 11 用例（TC-PRD-01~10 + TC-MST-1113 工序删除保护补测）；**跨 spec 数据隔离**（委外走 B-01 净变动 0 保 FIN-002 单行、TC-PRD-07 末盘亏复原 FIN-002<26 保 sales 并发超卖假设、盘点清补 MAT-001 不产生 PI/SOUT 行）；**顺带修复既有缺陷**：inventory.spec TC-INV-06 异步账面数竞态（加载后等行数）+ ProductionStructureTest PHPCS 样式违规；两份 E2E 文档已回填
  - **MySQL 迁移闭环（最终审查 load-bearing 项已关闭）**：本会话启动 Docker Desktop 29.6.2 + MySQL 8.4 容器，`php artisan migrate` 实跑成功（12 生产表 + 23 外键验证），此前「MySQL 开发库未启动」技术债消除

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 生产分支合入 main（当前待办）
- 状态: feat/production（627e129）已全绿，PR 待创建/合入
- **下一步具体动作**: `git push -u origin feat/production` → `gh pr create --base main` → `gh pr checks --watch` 云端 CI 全绿 → `gh pr merge --rebase --delete-branch` → 本地 `git fetch && git reset --hard origin/main` 对齐（参照销售 PR #5 流程）

### 2.2 统计报表模块（下一目标）
- 目标: 按主 spec §8/§11 与 `docs/superpowers/specs/2026-08-12-report-spec.md` 实施统计报表（库存报表/出入库汇总/生产统计/采购销售汇总），E2E `docs/test/2026-08-12-统计报表模块端到端测试.md` 全过
- 当前状态: 细化 spec 与 E2E 文档就绪；**实施计划未创建**；依赖链：生产模块（已完成）→ 报表
- 涉及文件/产物:
  - `docs/superpowers/specs/2026-08-12-report-spec.md` [待确认/修改] — 报表细化设计（错误码 1601 起）
  - `docs/test/2026-08-12-统计报表模块端到端测试.md` [待确认/修改] — 用例验收标准
  - `docs/superpowers/plans/2026-08-13-report.md` [待创建] — 实施计划（参照生产计划格式）
  - `server/app/Http/Controllers/Api/ReportController.php` 等 [待创建]
- 已锁定决策（生产模块沉淀，报表照抄）: 报表口径 = 流水/单据事实（库存恒等式、销售汇总、采购汇总、**生产统计：达成率/良率/工时/物料耗用**——数据源为报工记录 operation_reports、委外回收、BOM 展开快照 production_order_materials）；统一响应/分页/权限模式同前序模块
- **下一步具体动作**: writing-plans 生成实施计划 → subagent-driven-development 执行（**派发 implementer 时明确「不要运行 pint/prettier、不要 git add -A」，Task 提交前必跑全量 phpstan**）
- 验证方式: `php artisan test` 与 `npx vitest run` 全绿；E2E TC-REP 全过；云端 CI 全绿后 PR 合入 main

### 2.3 仪表盘模块（依次）
- 目标: 按主 spec §11 迭代 6 与 `docs/superpowers/specs/2026-08-12-dashboard-spec.md`（待审核单据数/预警三级联动/工单进度/库存总值/今日出入库）
- 当前状态: 细化 spec + E2E 文档就绪；未开始
- **下一步具体动作**: 报表模块完成后同一 SDD 流程

### 2.4 已知技术债（建议按序处理）
- **deferred minors 全集**（生产 ledger 已删，本文件留存）：N+1 查询 3 处（ReturnList/FinishedInbound index、release 缺料警告）、422 分支无测试 4 组（数量≤0/仓库库位缺失）、progress() 不钳上限、issue 幂等检查在事务外（无正确性影响）、pick_id 不交叉校验归属工单、complete() 工序校验无行锁（方向安全）、死锁 QueryException 兜底缺失（当前锁序同序理论不可达）、~~1116 守卫链未含 pick_list_items/return_list_items/finished_inbound_items 直接表~~（**2026-08-13 安全扫描修复波次已闭合**：ProductController 1116 链补齐 4 表含 inventory_check_items，见 docs/bugs/ B05 处理结论）
- **E2E 既有脆弱项**（销售模块沿用）：TC-SAL-07 PUT 硬编码 id、TC-SAL-08 依赖隐式排序、login 偶发超时（CI retries 2 兜底）；生产新增：TC-PRD-05/08 release warnings 空断言依赖前置库存结构、前端 :max 钳制断言绑定组件行为（与后端 API 断言双保险）
- **V1 预留扩展**：不良数返修/报废处置流程（报工页已标注）；委外工序与报工互斥（V1 不做，统计口径以报工记录为准）；发料分批发料（V1 一次发完）
- **`docs/bugs/` 目录**：2026-08-13 安全扫描修复波次已逐项处理并更新全部 11 份文档（B01-B11 处理结论 + 汇总表同步），状态与代码一致；「报工开工锁序反转死锁」经复核确认为已修复（锁序同序），笔记状态已更新

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本:
  - PHP 8.5.9 NTS x64（已入用户 PATH）、Composer 2.10.2（已入 PATH；**全局镜像为阿里云且不同步 Laravel 13，装包必须官方源**——项目 composer.json 已加 packagist.org 权威源，勿删）、Laravel 13.25.0（server/，Sanctum 4.3.3）、**MySQL 8.4 本会话已启动**（Docker Desktop 29.6.2，容器 php-design-mysql，db=php_design/user=php/pass=secret/port=3306，**生产 12 表迁移已实跑**）、**bcmath 扩展已验证可用**（数量整数运算依赖）
  - Node 24.18.0（web/，Vue 3.5 + Vite + Element Plus 2.14.4 + Pinia + Vue Router + axios）；Vitest 4 + @playwright/test 1.62（chromium 已装）
  - Python 3.14.6: `D:\code\envs\python\3.14.6\python.exe`（ui-ux-pro-max search.py 需完整路径调用）
  - CI 工具链（均已入库）：server/ — pint 1.30.5、php_codesniffer 4.0.4（PSR-12，phpcs.xml 含 5 条花括号豁免；**PHPCS 4 退出码：2=仅 warning（行长软限制）不阻断，CI 已 `|| PHPCS_STATUS=$?` 放行**）、larastan 3.10.0 + phpstan 2.2.8（level 5，需 `--memory-limit=1G`）；web/ — eslint 10 + typescript-eslint 8 + eslint-plugin-vue 10（flat config，**无 browser globals：禁用 setTimeout 用 nextTick**）、prettier 3、stylelint 17、husky 9 + lint-staged 17
- 关键配置:
  - `server/.env`（gitignore 不入库）: DB 已配 MySQL php_design；APP_URL=http://localhost:8000
  - `web/vite.config.ts`: 代理 `/api` → `http://127.0.0.1:8000`（2026-08-13 修复波次自 localhost 改，避免 IPv6 优先解析差异）；端口 5173；**无 resolve.alias（测试 import 必须相对路径，禁止 @/）**；vitest include 限定 `src/**/*.test.ts`
  - `web/src/main.ts`: Element Plus 已配 zh-cn locale
  - **playwright.config.ts 已设 `workers: 1`**（inventory/purchase/sales/production 共享库存数据必须串行，勿改回并行）；`retries: CI ? 2 : 0`；webServer 用 sqlite（`server/database/e2e.sqlite`，已 gitignore）自动 `migrate:fresh --seed`；**E2E 文件序 = 字典序：inventory < master < permission < production < purchase < sales < system（production 在 purchase/sales 之前，跨 spec 数据隔离必须维护）**
  - **注意**：phpunit 测试库为 sqlite（phpunit.xml，**sqlite 外键约束开启**——测试插外键数据须先建真实主数据）；**sqlite 唯一冲突错误码 19 ≠ MySQL 1062**（撞号重试已双码兼容）；**sqlite 将 DECIMAL 编译为 NUMERIC**（Schema 类型断言须兼容 decimal/numeric）；**phpunit 权限用例须 `$this->app['auth']->forgetGuards()`**（同 app 实例 guard 缓存）；**ApiResponse::ok() 已支持第三参 `$options`（JSON_PRESERVE_ZERO_FRACTION 局部启用，progress 浮点展示用）**
- 安装/构建/启动命令:
  ```bash
  cd D:/code/project/php-design
  docker compose up -d                                   # MySQL 8.4（Docker Desktop 需先启动）
  cd server && php artisan migrate --seed && php artisan serve   # 后端 :8000
  cd ../web && npm run dev                               # 前端 :5173
  cd server && php artisan test && cd ../web && npx vitest run    # 全量测试
  # CI 门禁（本地模拟云端；phpcs 退出码 2=仅 warning 放行；门禁勿与 E2E 并行——会饿死 vite 响应）：
  cd web && npm run type-check && npm run lint && npm run lint:css && npm run format:check && npm run test:unit
  cd ../server && vendor/bin/phpstan analyse --no-progress --memory-limit=1G && vendor/bin/phpcs -q; S=$?; [ $S -ne 0 ] && [ $S -ne 2 ] && exit $S; vendor/bin/pint --test && php artisan test
  cd ../web && npx playwright test                        # E2E（自动起后端种子库+vite，54 用例，~2.5m）
  ```

## 4. 全局约定 (Locked Conventions)
- **目录结构**: `server/`（Laravel 13 API，`/api/v1/*`，routes/api.php）；`web/`（Vue 3 前端）；`docs/superpowers/specs|plans/`；`docs/test/`（E2E 测试文档）；`docs/progress/`（本进度文档，每模块一份）；`design-system/nexus-factory/`（MASTER.md + pages/{master-data,inventory,purchase,sales,production}.md 页覆盖）
- **SDD 实施流程（不可跳过）**: 每模块 = writing-plans 生成计划 → subagent-driven-development 逐 Task 执行（TDD、每 Task reviewer、ledger 在 `.superpowers/sdd/`，用完即删）→ E2E 文档全过 → 最终全分支审查 → **PR 合入 main**
- **git 合入流程**: main 分支保护（strict status check「CI 汇总（全部通过）」+ enforce_admins），直接 push main 被 GH006 拒绝——必须：`git checkout -b feat/xxx` → push 分支 → `gh pr create --base main` → `gh pr checks --watch` 云端 CI 全绿 → `gh pr merge --rebase --delete-branch` → 本地 `git fetch && git reset --hard origin/main` 对齐
- **接口契约**: 统一响应 `{code,message,data}`（code=0 成功；422 仅格式层，业务冲突一律走业务码）；错误码：1001-1008 系统管理、1101-1124 基础资料（1113 工序被工单工序引用/1120 BOM 启用版本唯一/1121 BOM 被工单引用）、1201-1206 库存、1301-1312 采购、1401-1412 销售、**1501-1528 生产（1501 无启用 BOM/1502 数量业务码/1503-1504 已下达不可改删/1505 重复下达+关闭复用/1506 重复开工/1507 工序未完成或状态不可完工/1508 无入库/1509 不可报工/1510 合格累计超计划/1511 合格+不良超计划/1512 工时负/1513 领料超需求/1514 领料已审核/1515 商品[编码]库存不足/1516 领料重复审核/1517 退料超已领/1518 退料已审核/1519 退料重复审核/1520 委外超计划/1521 委外已审核/1522 委外库存不足/1523 委外重复审核/1524 回收超量/1525 入库超剩余/1526 成品不一致/1527 入库已审核/1528 入库重复审核）**、1601 起报表；权限中间件 `permission:{资源}.{动作}`（approve/close/release/start/complete/issue/回收 复用 update，报工提交复用 create）；分页 `{items,total,page,per_page}`；per_page 钳制 `max(1,min(100,(int)$request->input('per_page',10)))`
- **库存引擎（核心不变式）**: 一切库存变动（采购入库/销售出库/**领料/退料/委外收发/成品入库**/盘点）**必须**经 `InventoryService::apply`（事务双写流水+余额；movements 每条含 product_id/warehouse_id/location_id/direction(1|-1)/quantity(恒正)/source_type 枚举/source_id/source_no/remark；出库超卖抛 `InventoryException`；余额允许 0 不允许负；流水只增不改不删）；余额=流水求和恒等式
- **单据审核事务模式（生产模块沉淀，报表模块无审核；后续模块照抄）**: 事务内「锁单行（幂等判重）→ 逐行锁关联单据行（剩余量/状态复核）→ **逐行锁余额行校验余额充足（出库类单据；消息含商品编码与精确库存快照，尾零 `rtrim(rtrim($current,'0'),'.')`）** → InventoryService::apply（同事务 savepoint）→ 回写累计量（issued_qty/completed_qty bcmath）→ syncStatus/联动 → 置已审核+操作人+时间」；业务冲突抛模块专属 Exception（第二参数=业务码）由外层 catch 转 fail；**涉及数量/金额用 bcmath 整数运算**（bcmul/bcadd/bcsub/bccomp/bcdiv，BOM 展开 4 位中间精度）；validatePayload 中 quantity/price 加 `regex:/^-?\d+(\.\d{1,2})?$/`（防科学计数法 500，负值放行到业务码）；**nullable 数量字段必须 `?? 0` 归一化**（报工 E2E 文档化载荷 500 教训）
- **锁序全局约定（生产 Task 8 三连修复沉淀，**任何新事务必须遵守**）**: op/order 全系统同序——报工 op→next-op→order、委外回收 outsourcing→op→order、开工 op(seq1)→order、委外发出 outsourcing→order、完工/下达/关闭/成品入库 order only（工序一致性读不锁行）；**禁止任何 order→op 反向锁序**（ABBA 死锁 → 500）；新增事务先做环分析
- **单号规则**: `{前缀}{yyyyMMdd}-{3位}` 统一走 `DocumentSequenceService::nextNo(type, prefix, persist, legacyMax)`（document_sequences 表，type+date 唯一，FOR UPDATE 原子取号，删除不回退，撞号 1062/19 重试，老库 max 衔接）；已有 type：check/bom/po/pi/so/sout/**mo/pl/rl/os/osr/fi**（报表无单号；仪表盘无）；**禁止 count+1**
- **引用保护（DeletionGuard）**: 已生效——inventory_balances（1106/1107）、inventory_movements（1116）、purchase 系列（1109/1106/1107/1116）、sales 系列（1111/1106/1107/1116）、**production_orders.bom_id|product_id（1121/1116）、work_order_operations.process_id（1113）、production_order_materials.material_id（1116，生产接线）**；~~已知缺口——pick_list_items/return_list_items/finished_inbound_items 直接表未入 1116 守卫链~~（**2026-08-13 已闭合**：商品/仓库/库位/供应商删除守卫按迁移全量补齐，含盘点与生产单据全部 restrictOnDelete 表，见 docs/bugs/ B05/B08/B10）
- **RBAC**: 权限只授权给角色；新模块权限追加 RbacSeeder 权限数组（group=模块名），admin 自动全量持有、operator 自动持有全部 `%.list`；前端按钮用 `auth.has()` 门控
- **设计系统**: nexus-factory 为准（主色 #334155、强调绿 #059669、背景 #F8FAFC、Fira Code+Fira Sans）；页面骨架 `.page-card/.page-title/.toolbar/.btn-primary`；按钮文案全角空格「新 建/保 存/编 辑/删 除/审 核/关 闭/查 看/查 询/发 料/下 达/开 工/完 工/回 收/报 工」（E2E 按文案定位）；数量列 Fira Code 右对齐（formatYuan）；状态标签：工单五态（草稿灰/已下达蓝/生产中琥珀/**已完成深绿 tag-done**/关闭红）、工序三态（待开工灰/进行中蓝/已完成绿）、单据两态（草稿灰/已审核绿）、委外三态（草稿灰/已审核蓝/已回收绿）、发料三态（未发料灰/部分发料琥珀/全部发料绿）；主弹窗 900px（**次级弹窗豁免：缺料警告 560/委外回收 560/回收记录 700——production.md 已注明**）；扫码自动聚焦（命中清空/未命中保留；**生产模块 V1 无扫码入口**）；页覆盖规范见 `pages/{inventory,purchase,sales,production}.md`
- **用户硬性约束（AGENTS.md）**: 全部注释/日志/文档中文；UTF-8 无 BOM；LF 行尾（.gitattributes 已强制）；无死代码（未使用 import 零容忍）；核心路径（认证/权限/**库存一致性/审核并发/数量 bcmath/锁序**）100% 单测；E2E 未过不合并
- **工程纪律（本会话教训）**: 严禁手动运行 pint/prettier（pre-commit 钩子 lint-staged 自动修复属正常）；SDD 派发 implementer 时明确「不要运行 pint/prettier、不要 git add -A」；**Task 提交前必跑全量 phpstan（0 错误）**；**门禁勿与 E2E 并行（同机 CPU 争用会饿死 vite → login 30s 超时）**；phpunit 权限用例须 forgetGuards；计划文件生成用 Write/Edit 分块（heredoc 易截断）；el-input 组件实例无 value 属性（扫码取值必须 v-model）；eslint 无 browser globals（禁用 setTimeout，用 nextTick）；edit 流程必须区分 create/update 走不同 API；**vue-tsc 对缺失视图动态 import 报 TS2307——前端路由 Task 与页面 Task 必须同批验证门禁**
- **云端 CI**: `.github/workflows/ci.yml` 在 push main / PR 到 main 时跑后端（Pint→PHPCS→PHPStan→PHPUnit）+ 前端（vue-tsc→ESLint→Stylelint→Prettier→Vitest）+ E2E（Playwright，retries 2）+ 汇总 job；main 分支保护要求「CI 汇总（全部通过）」strict 检查
- **环境杂项**: Docker Desktop 本会话已启动（29.6.2）；`.zcode/` 与 `auth.json` 已 gitignore

## 5. 后续会话标记（2026-08-13 安全扫描问题修复波次）

> 本会话（生产模块已合入 main 之后）按 `docs/bugs/2026-08-13-问题汇总.md` 执行安全扫描问题修复，详情与待办见新进度文件 **`docs/progress/2026-08-13-安全扫描问题修复.md`**。要点：

- 修复 9 项（B01 admin 提权 / B02 角色保留码 / B03 回填竞态 / B04 种子幂等 / B05+B08+B10 删除守卫 / B09 CSV 流式 / B11 低优先级 4 项），标注 2 项（B06 此前已修、B07 判定不存在）；回归全绿（后端 375 单测 + phpstan 0 + pint/phpcs；前端 vue-tsc/eslint/prettier/vitest 61）
- **已合入 main**：提交 1dab666，PR #8，云端 CI 全绿（后端/前端/E2E/汇总）
- **B07 账外盘盈：用户裁决「暂不做」**（无余额商品不可录盘为一致性设计；如需支持按 B07 文档改动点清单实施）
- 本文件 §2.4/§4 的 1116 守卫缺口与 §3 的 vite 代理 localhost 条目已随本次修复更新，勿再按旧文处理
- 生产模块后续延续本文件 §2（统计报表模块）不变
