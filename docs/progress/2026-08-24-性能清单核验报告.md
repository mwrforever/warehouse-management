# 2026-08-24 性能清单核验报告（只读核对，不做修复）

> 核验日期：2026-08-25
> 核验对象：`docs/pref/2026-08-24-数据库查询性能审查.md`（P-1~P-4 + 待裁决 D-1/D-2 + 缓存评估 + 索引核查结论）与
> `docs/pref/2026-08-24-IO与前端性能审查.md`（PF-1~PF-3 + 待裁决 PJ-1）
> 核验方式：L1 事实源逐条比对——精读条目引用的 文件:行号 原文、`git show`/`git log` 验证提交证据
> （d7d3acb/d7d3acb~1/b03b989/88c8711/936b4ed/8241c8e 均已在库）、迁移 DDL 索引逐条核对、
> grep 横向复核（apply 调用方 / inventory_movements 裸表操作 / source_type 筛选 / 批量预锁分布）。
> 未连库、未执行 EXPLAIN（与被核验清单口径一致，涉及推演处下方注明）。
> 判定口径：✅ 真实（现状与方案均成立）｜ ❌ 伪（代码事实反驳）｜ ⚠️ 证据不足。

---

## 一、数据库查询性能审查清单（P1）核验

### P-1【中】委外发出（approve）扣减校验：循环内逐组件带锁点查余额行 —— 【核验:✅真实】

**核验过程**：
- 读 `server/app/Http/Controllers/Api/OutsourcingController.php` 全文。`:380` 为 `foreach ($locked->items->sortBy('material_id') as $item)`，`:381-384` 为 `InventoryBalance::where('product_id',...)->where('warehouse_id',...)->where('location_id',...)->lockForUpdate()->first()`——条目贴出的代码为原文，行号精确吻合（条目标注 :380-405，实际循环体 380-405，`$movements = []` 在 :379，误差 0~1 行内）。
- `:407-409` 的 `apply($movements, ...)` 调用与条目引用一致；读 `server/app/Services/InventoryService.php:51-55` 确认 `applyOne` 对每条 movement 再执行一次同谓词 `lockForUpdate()->first()`——"扣减段合计 ~2N 次余额行查询"的推算成立（控制器 N 次 + 引擎 N 次幂等重入）。
- git 证据：`git show d7d3acb~1:...OutsourcingController.php` 旧版 approve 仅一条成品余额行锁（`InventoryBalance::where('product_id', $order->product_id)->...->lockForUpdate()->first()` 单条查询）；当前版本为循环组件锁——"d7d3acb 引入"属实，且确为新写代码沿用旧模式（非历史遗留）。
- 先例核验：`PickListController.php:295-313` 批量预锁代码原文在位（注释含"批量锁按索引序获取与逐行等价，消除交叉锁窗口"的锁序论证）；`SalesOutboundController.php:353-360`、`ReturnListController.php:269`、`PurchaseInboundController.php:356/361` 同款批量锁均存在——"08-16 P1-2 四类审核已批量化"说法有代码支撑。
- 索引事实：`balance_unique(product_id, warehouse_id, location_id)` 见 `2026_08_12_090000_create_inventory_tables.php:22`，方案中 whereIn 批量锁走该唯一索引的前提成立。

**反驳证据**：无。

**宪法一致性核验**：方案与 §4.2.4【建议】（whereIn 批量预取）直接对应；锁序论证成立——whereIn + 等值 warehouse/location 走 `balance_unique` 索引序（product_id 升序）获取，与现行 `sortBy('material_id')` 逐行升序等价单调，且批量锁仍在「委外单(:343) → 工单(:359) → 余额行」链条末端，全局锁序「单据头→明细→库存」（§4.3.2）不变，无新 ABBA 方向；与已落地的 PickList/SalesOutbound 先例同构同论证。收益"N→1"未夸大（每次循环确为 1 次独立往返）。

### P-2【中·随业务量确定性退化】回收/余料退回流水单号回补 UPDATE 仅命中 source_type 单列索引 —— 【核验:✅真实】

**核验过程**：
- `OutsourcingController.php:521-525`（storeReceipt）与 `:679-683`（storeReturn）的 `DB::table('inventory_movements')->where('source_type',...)->where('source_id',...)->where('source_no','')->update(...)` 与条目贴码逐字一致。
- 索引事实：`2026_08_12_090000_create_inventory_tables.php:41-42` 定义 `movement_product_wh(product_id,warehouse_id)` 与 `movement_source_type(source_type)`；`2026_08_12_103000` 增加 `movement_created_at(created_at)`；遍历全部 23 个迁移确认 `inventory_movements` 无任何 `source_id` 索引——"只能走 source_type 单列索引、回表过滤"的前提是 DDL 铁的事实（执行计划为基于索引定义的推演，清单已自行注明未跑 EXPLAIN，推演方向正确）。
- "流水表只增不改不删"：`InventoryService.php:97` 注释原文"流水只增不改不删（审计要求）"；每笔回收 1 条 outsourcing_in 流水（storeReceipt 单条 apply），扫描面随该类型流水总量线性增长的推演成立。
- 方案 a 前提：grep 全项目 `inventoryService->apply` 共 9 处调用，其中非委外调用方恰为 6 处（Check:293/PurchaseInbound:400/FinishedInbound:299/PickList:349/SalesOutbound:410/ReturnList:297）——"既有 6 个调用方"数字精确；PHP 忽略返回值不改语义成立。
- 方案 b 前提：`InventoryController.php:83-84` 与 `ReportService.php:141` 的 `source_type` 单列筛选确实存在（新复合索引最左前缀可继续服务）；替换式复合索引先例迁移 `2026_08_15_160000_add_composite_index_to_purchase_inbound_items.php` 在库。

**反驳证据**：无。

**宪法一致性核验**：方案 a 零索引变更、不改锁序、主键 whereIn 点查，无缓存无一致性负担；方案 b 用 `(source_type,source_id)` 替换单列索引避免冗余（§6.3），且 `(source_type,source_id)` 服务等值前缀查询不违反最左前缀与"%xxx% 禁建"条款。两方案均合规，推荐顺序（a 优先）合理。

### P-3【低】委外发出：零组件防线 COUNT 与组件预载两次查询 items 表 —— 【核验:✅真实】

**核验过程**：`:355` 为 `if ($locked->items()->count() === 0)`（原文），`:376` 为 `$locked->load('items.material')`（原文）。两次独立查询 items 表的事实成立（count() 走查询构造器、load 是第二次）。

**反驳证据**：无。

**宪法一致性核验**：`isEmpty()` 与 `count()===0` 语义等价（Laravel 集合层事实）；合并后行为不变、无锁序影响，符合 §4.2.2 减少重复查询方向。收益"省 1 次常数查询"表述克制无夸大。

### P-4【低】余料退回：全退判定重复查询 items 表 —— 【核验:✅真实】

**核验过程**：
- `:621` `$items = $locked->items()->lockForUpdate()->get()->keyBy('id')`、`:686-689` foreach 更新 `returned_qty` 并 save、`:691-692` `$allReturned = $locked->items()->get()->every(...)`——三段均为原文，行号吻合。
- 同实例前提验证：`:627` `$item = $items->get($line['item_id'])` 从已锁集合取同一模型实例，`:687` 更新的即该实例内存值；`:621` 与 `:691` 查询均无过滤条件（同单全量 items），`$items->every(...)` 与重查结果逐字段等价的论证成立。

**反驳证据**：无。

**宪法一致性核验**：复用已锁集合消除重查与 PickList:350-356"复用已锁行对象"既有定式一致（§4.2.2）；不改变任何锁获取行为，无宪法冲突。

### D-1【待裁决】工艺路线保存逐行 INSERT —— 现象描述【核验:✅真实】

**核验过程**（对待裁决项仅核验现象真实性，不替用户裁决）：
- `RoutingService.php:226-239` 原文吻合：节点逐行 `$routing->nodes()->create(...)`（需回填 `$node->id` 建边映射）、材料逐行 `$node->materials()->create($m)`、边逐行 `$routing->edges()->create(...)`。
- 先例引用核实：`ProductionOrderController::store` 的 `:155`（operations createMany）与 `:171`（edges createMany）确实存在。
- 量级描述（节点<20、材料<50、边<40）为业务口径推断，代码层无法核验，但不影响现象真实性。

**反驳证据**：无。

**宪法一致性核验**：方案 a（createMany）与项目既有惯例对齐、同事务全量替换语义不变；方案 b（维持）亦与简单优先（1.1）相容——两案均无宪法冲突，维持"待裁决"定位恰当。

### D-2【待裁决】委外节点取数全路线预载 —— 现象描述【核验:✅真实】

**核验过程**：
- `OutsourcingService.php:36-37`（fromOperation）与 `:83-87`（routingNodeForOperation）原文吻合：`RoutingHeader::with('nodes.materials.material','nodes.materials.unit')->findOrFail(...)` 全路线预载后 `firstWhere('node_no',...)` 内存取单节点。
- "5 查"推演核对：该 with 链 = header 1 + nodes 1 + materials 1 + material 1 + unit 1 = 5 条查询，数字准确；方案 a 所依赖的 `uniq_routing_nodes_node_no(routing_id,node_no)` 索引在 `2026_08_24_100000:41` 确认存在。
- 两处同构、store/update 会重复调用的说法与 `OutsourcingController::store:127/update:255` 调用点一致。

**反驳证据**：无。

**宪法一致性核验**：方案 a 单节点直查（firstOrFail + catch 保持 422 语义）不违反任何宪法条款；方案 b 维持亦合规。"待裁决"定位恰当。

### P1 清单 §四 缓存机会评估 —— 【核验:✅真实】（结论依据成立）

**核验过程**：
- `RoutingService::graph`（:263-295）存在且 `load` 8 个关系路径（product/nodes.materials.material/nodes.materials.unit/nodes.process/nodes.outputProduct/edges.fromNode/edges.toNode）——实际查询数约 10 条（头行已持有不再查），条目写"约 11 条"轻微高估 1 条，量级判断不受影响（见文末附带发现①）。
- 失效点分析核实：persist（:181 全量替换）/toggle（:252）/delete（:246）三个写路径确实存在，"失效点多且分散"属实。
- `expandBom` 的启用工序全集：`ProductionOrderService.php:34-38` `Process::query()->where('status',1)->orderBy('sort')->orderBy('id')->get()` 原文在位。

**反驳证据**：无。

**宪法一致性核验**："不推荐引入缓存"的结论与一致性维护评估自洽——失效点多、读频低、单次成本低，引入缓存反而制造一致性负担；无"缓存方案一致性维护是否完整"的缺口（因为结论是不引入）。

### P1 清单 §五 索引核查结论 —— 【核验:✅真实】

**核验过程**（逐行对照迁移 DDL）：
- `2026_08_24_100000`：`code` unique（:16）/ `routing_headers(product_id)` 普通索引（:24）/ `uniq_routing_nodes_node_no`（:41）/ `uniq_routing_node_materials`（:52）/ `uniq_routing_edges_triple`（:62）——与 §五 表格逐项一致。
- `2026_08_24_100100`：`uniq_work_order_operations_node_no(order_id,node_no)`（:31）/ `uniq_work_order_operation_edges(order_id,from,to)`（:44）；`work_order_operations_seq_unique(order_id,seq)` 在 `2026_08_14_100000:52`——§五 引用准确。
- `2026_08_24_200000`：`uniq_outsourcing_order_items(outsourcing_id,material_id)`（:30）/ `idx_outsourcing_returns_pair`（:50）——一致。
- FK 自动索引推演前提核实：`outsourcing_orders.operation_id` 为 `->constrained()` 外键（`2026_08_14_110000:70`）且表内仅有 `status`/`order_id` 显式索引（:79-80）无 operation_id 显式索引——推演前提成立；条目已如实注明"基于 InnoDB 文档化行为推演，未连库验证"，属合理推演非臆断。
- `%xxx%` 禁建索引口径：`RoutingController:33` 的 `where('code','like',"%{$keyword}%")` 与 `OutsourcingController:63` 的单号 like 属实，维持禁建结论符合 §6.3。

**反驳证据**：无。

**宪法一致性核验**：全部索引结论与 §6.3（最左前缀、禁 %xxx% 索引、禁冗余）一致；`idx_outsourcing_returns_pair` 与 FK 需求复用不冗余的说法与 InnoDB 行为相符。

---

## 二、IO 与前端性能审查清单（P2）核验

### PF-1【中】委外发出 approve：组件余额行循环内逐条锁查询 —— 【核验:✅真实】

**核验过程**（与 P-1 为同一代码事实，两份清单交叉一致）：
- 现状代码引用（:379-405）逐行比对为原文（含 `?? '0'` 短路与 1522 消息含商品名逻辑，均在 :385-392）。
- 引入提交验证：`d7d3acb` 提交信息含 "feat(outsourcing): 发出扣组件库存（分量流水/全量回滚）"；`d7d3acb~1` 旧版 approve 为单条成品余额行锁（单条 lockForUpdate 查询）——"旧版单条、本次重构写成循环逐条"的表述属实。
- 先例代码验证：`PickListController.php:295-313` 批量预锁及其锁序论证注释原文在位（任务提示中提到的 :298-313 亦指向同一代码块，:298 为 `$productIds` 行）；`SalesOutboundController.php:353-360` 同款在位。
- `apply()` 内重入锁幂等：PickList:348 注释"余额行已被本事务锁定，引擎内重复加锁幂等"与 InventoryService 实现一致。

**反驳证据**：无。唯一未能溯源的支撑性表述是"（同索引序获取）有生产运行记录"（见附带发现②），不影响主体事实与方案。

**宪法一致性核验**：同 P-1——批量预锁走 `balance_unique` 索引序获取，与 `sortBy('material_id')` 等价单调，锁序链条「委外单 → 工单 → 组件余额行（material_id 升序）」不变，符合 §4.3.2 全项目锁序；收益区间（典型 2~5 组件省 1~4 条查询）与代码事实吻合，未夸大。

### PF-2【中】回收/退回「空串占位 + 流水单号回补 UPDATE」可重排消除 —— 【核验:✅真实】

**核验过程**：
- 现状引用：storeReceipt `:491-525`（apply :491-501 携 `'source_no' => ''` → nextNoByConfig :503-519 → 回补 UPDATE :521-525）与 storeReturn `:654-683` 同构——逐行比对均为原文，行号吻合。
- 索引事实：同 P-2 已核（三个索引、无 source_id 索引）——机理 1（类型分区范围扫描 + 回表）前提成立。
- "全项目唯二"验证：grep 全项目 `DB::table('inventory_movements')` 仅命中 OutsourcingController:521/679 两处——"唯二例外"属实。
- 惯例对照穷尽性抽查：PickList `:283`（锁单）→ `:349`（apply，source_no=$locked->no 真实单号）；SalesOutbound `:333` → `:410`（source_no 真实单号 :405）；FinishedInbound store `:117` 取号建单 → approve `:261/266/299` apply；ProductionOrder store `:107` 锁成品 → `:126` 取号——"其余路径均为单据/序列锁在前、余额行锁在后"的对照结论有充分代码支撑，现行 storeReceipt/storeReturn 确为唯一"先 apply 后取号"反向样本。
- 契约验证：`DocumentSequenceService` docblock"须在调用方 DB::transaction 内调用"确实存在（实际行位 :20，条目写 :21，偏差 1 行非实质）；重排后仍在 storeReceipt/storeReturn 外层事务（`,2` 在位）内，契约满足。
- 死锁重试幂等：`DB::transaction(fn, 2)` 重跑闭包时未提交流水随回滚撤销、重新写入——表述与 Laravel 事务语义一致。

**反驳证据**：无。

**宪法一致性核验**：重排将 document_sequences 行锁与回收单插入从"余额行锁之后"提前到"之前"，方向是向全项目主流锁序收敛（消除唯一反向样本），跨路径单调性增强、无新增 ABBA 方向，符合 §4.3.2；顺带消除的 movements 扫描行锁竞争窗口属正向收益；审计链等价（最终 source_no 相同）。备选方案（复合索引）需按 §6.3 评审冗余性，条目对备选劣于重排的判断合理。

### PF-3【低中】回收弹窗每次打开重复拉全量委外详情 —— 【核验:✅真实】

**核验过程**：
- `web/src/views/production/OutsourcingsView.vue:396-413` openReceipt 原文吻合：`await productionApi.outsourcingDetail(row.id)` 后仅消费 `d.quantity / d.received_qty / d.output_product_name` 三字段（:400-401）。
- 列表已含三字段验证：后端 `OutsourcingController.php:90`（output_product_name join）/`:93`（quantity）/`:95`（received_qty withSum 归一）；前端 `api/production.ts` 的 `OutsourcingItem` 类型含 `output_product_name?`（:204）/`quantity`（:207）/`received_qty?`（:209）——方案"直接用 row 三字段"类型层面可行。
- detail 确为重路径：`OutsourcingController::show:193` 预载 4 组关系 + `:219` receivedQty SUM 查询。
- 兜底验证：storeReceipt 事务内超收校验 1524 在位（:474-476），"列表数据滞后仅影响预填值、提交有权威兜底"的分析成立。

**反驳证据**：无。

**宪法一致性核验**：纯前端取数来源替换，无缓存无失效问题；后端 received_qty 列表/详情双口径已归一（均 bcmath 2 位，index :95 与 receivedQty :747），与 §5.7 契约一致。收益"每次开弹窗省 1 次重查询"属实无夸大。

### PJ-1【待裁决】工艺路线画布弹窗每次开窗重拉 4 个下拉请求 —— 现象描述【核验:✅真实】

**核验过程**（仅核验现象真实性，不替用户裁决）：
- `web/src/components/routing/RoutingCanvasDialog.vue:439-476`：`watch(props.visible)` 每次开窗执行 `Promise.all` 拉工序全量 + 三类商品 per_page=100 共 4 请求（:452-457 原文吻合）；watch 每次触发均重拉（含编辑↔查看来回切换）属实。
- 会话序号守卫在位（:436-475，开窗/关窗递增作废在途响应）——条目"会话守卫已防竞态"属实。
- 方案 B 的失效复杂度论证（跨页面失效需全局 store/事件总线，违反简单优先 1.1）逻辑成立。

**反驳证据**：无。

**宪法一致性核验**：方案 A（维持）零成本零风险；方案 B（缓存）确有一致性维护缺口（跨页失效）——条目按"无法低成本保证一致性即不推荐"口径标注，与缓存一致性审慎原则相符。建议倾向 A 供用户裁决，定位恰当。

---

## 三、低风险备注区抽查（N-A~N-D，非必须判定项，轻量核对）

| 编号 | 抽查结果 | 判定 |
|---|---|---|
| N-A | `api/production.ts` 的 outsourcingReceipts/outsourcingReturns/operationReports 确不传 per_page；后端 `OutsourcingController:579/716`、`OperationReportController:161` 默认 10 | ✅ 事实成立 |
| N-B | `InventoryService.php:91` `$balance->quantity = (float) $balance->quantity + $direction * $quantity;` 原文在位 | ✅ 事实成立 |
| N-C | `OutsourcingsView.vue:587-596` onMounted 三连串行 await（warehouses→suppliers→orders）在位 | ✅ 事实成立 |
| N-D | `InventoryController.php:87-89` source_no 精确筛选在位且无覆盖索引 | ✅ 事实成立 |

---

## 四、统计汇总

| 区块 | 条目 | 判定 |
|---|---|---|
| P1 确认优化区 | P-1 / P-2 / P-3 / P-4 | 全部 ✅ 真实（4/4） |
| P1 待裁决区 | D-1 / D-2 | 现象描述均 ✅ 真实（2/2） |
| P1 缓存评估 | 2 候选均不推荐 | ✅ 结论依据成立 |
| P1 索引核查 | §五 全表 | ✅ 与迁移 DDL 一致 |
| P2 确认优化区 | PF-1 / PF-2 / PF-3 | 全部 ✅ 真实（3/3） |
| P2 待裁决区 | PJ-1 | 现象描述 ✅ 真实（1/1） |
| P2 低风险备注 | N-A~N-D | 抽查全部 ✅（4/4，非必须项） |

- ❌ 伪：0 项；⚠️ 证据不足：0 项。
- 宪法一致性：全部确认优化方案（P-1~P-4、PF-1~PF-3）经 §4.2/§4.3/§6.3 逐条核验无冲突；P-1/PF-1 锁序等价单调论证与既有已落地先例（PickList:295-297 注释）同构；PF-2 重排方向为向全项目统一锁序收敛。
- 收益评估：未发现夸大——各条目收益区间（N→1、省 1 次常数查询、随流水线性退化的消除）均可由代码事实直接支撑。
- git 证据：d7d3acb / d7d3acb~1 / b03b989 / 88c8711 / 936b4ed / 8241c8e 提交均在库且与条目引用内容相符。

## 五、核验附带发现（不升级为新问题，仅记录）

1. **计数轻微偏差（不影响结论）**：P1 清单 §四称 graph"单次约 11 条小表查询"，实际 `load` 8 个关系路径约发 10 条查询（头行由路由绑定已持有不再查）。量级判断（低频小结果集、不推荐缓存）不受影响。
2. **不可溯源的支撑性表述**：PF-1 中"（同索引序获取，无 ABBA 新方向）……有生产运行记录"——"生产运行记录"在代码与 git 历史中无可验证的对应物（应为历史评审/运行文档口径）。主体事实（注释论证在位）不受影响。
3. **前端类型细节（方案实施提示）**：`OutsourcingItem.received_qty?` 为可选属性（`api/production.ts:209`），PF-3 方案落地时直接用 `row.received_qty` 在 TypeScript 严格模式下需按可选值处理（后端实际恒返回，运行时无虞）——实施时的类型小坑提示，非方案缺陷。
4. **行号微小偏差（均非实质）**：PF-2 引用 `DocumentSequenceService.php:21` 的契约注释实际内容行在 :20（docblock 首行内容）；P-1 标题段":380-405"实际循环自 :380 起、`$movements=[]` 在 :379。以上偏差均 ≤1 行且指向唯一无歧义代码。
