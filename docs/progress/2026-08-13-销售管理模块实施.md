# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\php-design
- saved_at: 2026-08-13T12:00:00+08:00
- 版本控制提交号: d5844d7（main，已对齐 origin/main；销售模块经 PR #5 rebase 合入）
- 任务一句话描述: 进销存+生产管理系统（Nexus Factory）——按 superpowers SDD 流程实施**销售管理模块**（销售订单→销售出库单两级单据，出库单审核事务内锁余额行防超卖、经 InventoryService 写 sales_outbound 流水扣库存、回写 shipped_qty 联动订单状态）已完成并全绿合入 main；下一目标为生产管理模块

## 1. 已完成 (Completed) — 简要标记
- [x] 销售管理模块完整实施（11 commits `1ea4001..d5844d7`，SDD 流程 8 Task 逐 Task 审查 + 1 轮修复循环 + 最终全分支审查修复波次闭环；PHPUnit 260（810 断言）、E2E 43/43、phpstan 0、phpcs/pint 全绿；云端 CI 四项全绿后 PR #5 rebase 合入）：
  - 后端：4 表迁移（sales_orders/sales_order_items/sales_outbounds/sales_outbound_items，金额 decimal(14,2) 分单位、shipped_qty 回写列、order_id/order_item_id 可空 nullOnDelete）+ 4 模型（SalesOrder 五态常量+STATUS_LABELS、SalesOutbound 两态）+ 8 项权限（sales.order/inbound... 实为 sales.order/outbound，group=销售管理；approve/close 复用 update）+ **DocumentSequence 追加 TYPE_SO/TYPE_SOUT 常量（零迁移）**
  - **SalesOrderController**（草稿 CRUD **update/destroy 事务内锁行复查 1402/1403** + approve 1404 + close 1405 + available + outbounds 记录；错误码 1401-1405/1411/1412；available 路由须在 {order} 前）+ SalesOrderService（lineAmount/calculateTotal bcmath 整数分 + syncStatus 订单状态重算）+ SalesException
  - **SalesOutboundController::approve 核心不变式**：事务内「锁单幂等 1410 → 逐行锁订单行+订单头（状态校验/剩余量 bcmath 复核 1407）→ **逐行锁余额行校验余额充足（1409 消息含商品名+精确库存快照，尾零已修）** → InventoryService::apply 写 sales_outbound 流水(-1)+扣余额 → 回写 shipped_qty → syncStatus 重算」任一步失败整体回滚；错误码 1406-1412（1406 仓库库位必填/1407 超剩余量+客户一致性/1408 已审核不可改删/1409 库存不足/1410 幂等/1411 负价/1412 重复商品）；fromOrder 预填剩余量；**todaySummary 当日已审核出库量按商品汇总**（列表页汇总行数据源）；**库存快照尾零处理 `str_contains($current,'.') ? rtrim(...) : $current`**（缺余额行 '0' 原样输出）
  - **删除保护联动补全（Task 2）**：Customer 1111（sales_orders+sales_outbounds 双查）、Warehouse 1106、Location 1107、Product 1116（补 sales_outbound_items）各有测试；CustomerTest 临时表测试改造为真实表（sales_orders 迁移落地后原 Schema::create 会撞表名）
  - **技术债清债（Task 1）**：采购订单 update/destroy 事务内锁行复查（1303/1304，最终审查 out-of-scope 遗留，与入库单 C1 同构）
  - 前端：2 页（`OrdersView.vue` 五态标签/原料禁售下拉（finished+semi_finished 两次调用合并）/扫码/详情+出库记录 tab；`OutboundsView.vue` 双入口「从订单生成/新 建」/预填/审核 confirm「库存将减少」/**1409 红色错误提示**/今日出库汇总行/route.params.id 直达）+ `salesApi` 17 方法 + 2 路由 + 「销售管理」菜单组 + 设计系统页覆盖 `design-system/nexus-factory/pages/sales.md`（ui-ux-pro-max 查询驱动）
  - E2E：`web/e2e/sales.spec.ts` 9 用例（TC-SAL-01~08 + TC-MST-05 客户删除保护补测 1111）；**E2E 驱动修复 2 个既有缺陷**：MovementsView gotoSource 补 sales_outbound 跳转分支、movements 接口补 source_no 筛选参数（各带回归测试）；两份 E2E 文档 §5 已回填
  - 最终审查修复波次（dc92f2e）：1409 缺余额行消息尾悬空（str_contains 分支）+ sales.md 未实施项标注

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 生产管理模块实施（下一目标）
- 目标: 按 `docs/superpowers/specs/2026-08-12-production-spec.md` 实现生产工单+工序报工+委外收发+成品入库（「制单→审核」状态机，BOM 展开、成品入库联动工单完成），E2E TC-PRO-xx 全过
- 当前状态: 细化 spec 与 E2E 文档就绪（`docs/superpowers/specs/` 与 `docs/test/` 目录）；**实施计划未创建**；依赖链：基础资料 → 库存 → 采购 → 销售（已完成）→ 生产
- 涉及文件/产物:
  - `docs/superpowers/specs/2026-08-12-production-spec.md` [待确认/修改] — 生产模块细化设计
  - `docs/test/2026-08-12-生产管理模块端到端测试.md` [待确认/修改] — 用例验收标准
  - `docs/superpowers/plans/2026-08-12-production.md` [待创建] — 实施计划（参照销售计划格式：技术债 → 迁移+模型 → 状态机 TDD → 前端 → E2E）
  - `server/app/Models/DocumentSequence.php` [修改] — 追加 TYPE_MO/TYPE_RO 等常量（零迁移）
  - `server/database/migrations/*` [待创建] — production_orders/work_order_operations/outsourcing 等表
- 已锁定决策（来自销售模块沉淀，生产照抄）: 单据「制单→审核」两级状态，审核后库存才变动；生产工单 BOM 展开、工序报工、委外收发、成品入库联动工单完成；单号 `{前缀}{yyyyMMdd}-{3位}` 走 DocumentSequenceService（加 type 常量即可，零迁移）；**生产表落地后 1113 工序/1121 BOM 删除保护自动生效**（DeletionGuard 已接线，E2E 顺带补测）
- 阻塞点/风险: 生产模块业务复杂（BOM 展开/多工序/委外），建议 Task 拆分更细；**销售模块已知技术债见 2.3**
- **下一步具体动作**:
  1. 用 superpowers:writing-plans 生成实施计划：`docs/superpowers/plans/2026-08-12-production.md`（先核对生产 spec 错误码段 1501-1528，再迁移+模型+状态机 TDD，再前端+Vitest+E2E）
  2. 用 superpowers:subagent-driven-development 执行（每 Task 派发 implementer + reviewer，ledger 在 `.superpowers/sdd/`，用完即删；**派发 implementer 时明确「不要运行 pint/prettier、不要 git add -A」，且 Task 提交前必跑全量 phpstan（销售模块 3 次教训：Task 3/4 implementer 未跑 phpstan 导致 13 错误遗留到最终门禁）**）
  3. E2E 顺带补测基础资料 **1113 工序删除保护**（process 表已建，production 表落地后自动激活）
- 验证方式: `php artisan test` 与 `npx vitest run` 全绿；E2E TC-PRO 全过（web/e2e/production.spec.ts）；云端 CI 全绿后 PR 合入 main（见 §4 流程）

### 2.2 报表/仪表盘模块（依次）
- 目标: 按主 spec §11 迭代 5-6 依次实施（报表→仪表盘），每模块走同一 SDD 流程
- 当前状态: 细化 spec + E2E 文档全部就绪（`docs/superpowers/specs/` 与 `docs/test/`）；未开始
- 已锁定决策: 报表口径 = 流水/单据事实（库存恒等式、销售汇总、采购汇总）；仪表盘 = 待审核单据数/预警三级联动
- **下一步具体动作**: 按顺序逐模块 writing-plans + subagent-driven-development
- 验证方式: 各模块 E2E 文档 TC 全过；跨模块联动（流水单号跳转、预警三级联动、报表口径一致）按各文档 §4 判定

### 2.3 已知技术债（建议按序处理）
- **E2E 登录导航偶发超时（本机环境抖动）**：三次全量 E2E 中两次各有 1 个既有库存用例在 `loginByAPI` 的 `page.goto('/login')` 30s 超时（TC-INV-10 / TC-MST-03，不同用例、均为登录导航），与销售改动无关；本机 MindSoar basedpyright 偶发吃内存（handoff 已知）。CI 已配 `retries: 2` 兜底；本地复现可重跑。**不建议为 flutter 加超时**——若频繁可考虑将默认 test timeout 提到 60s（inventory.spec 未设 60s，purchase/sales 已设）
- **并行跑门禁会饿死 E2E**（本会话教训）：PHPUnit/Pint 与 playwright 同机并行 → vite 响应 >30s → login 超时。门禁顺序执行，勿并行
- **from-order 弹窗无删除明细行能力**（产品决策点，与采购同源）：TC-SAL-02 首张出库单必然含全部剩余行（SEMI-001×5 随单出完），TC-SAL-05 预填仅 FIN-002×4 一行；E2E 文档已记录。是否给「从订单生成」明细加行删除按钮（限定至少保留一行）需产品拍板
- **from-order 模式扫码框仍渲染**：可混入无 order_item_id 的独立行（后端设计如此）；建议 `v-if="mode === 'standalone'"` 一行修复（采购手记同款遗留）
- **E2E 脆弱断言**（deferred）：TC-SAL-07 PUT 硬编码 id（依赖 1408 先于载荷校验）；TC-SAL-08 semiAfter.items[0] 依赖余额接口隐式排序（A-01 < B-01）；expect(s2b).toBe(50) 绝对断言（依赖 migrate:fresh）
- **deferred minors 全集**：见销售计划 ledger（已删）——含 update/destroy firstOrFail 窄窗口 500（与入库单 C1 同构接受）、ABBA 理论死锁（同载荷锁序一致接受）、扫码路径不过滤原料（后端 422 兜底）、sales.md §1 单号列点击/dirty 二次确认未实施（已标注）、approve 预检对已完成/关闭返回 1404 文案（与采购 1305 一致取舍）、PHP 8 不带 items 键 undefined key 警告（语义正确）、Product::find N+1
- **MySQL 开发库**：本会话期间 Docker MySQL 未启动（`php artisan migrate` 未在开发库实跑，迁移经 sqlite 全量重迁验证；`docker compose up -d` 后补跑一次 `php artisan migrate` 即可）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本:
  - PHP 8.5.9 NTS x64（已入用户 PATH）、Composer 2.10.2（已入 PATH；**全局镜像为阿里云且不同步 Laravel 13，装包必须官方源**——项目 composer.json 已加 packagist.org 权威源，勿删）、Laravel 13.25.0（server/，Sanctum 4.3.3）、MySQL 8.4（Docker 容器 php-design-mysql，db=php_design/user=php/pass=secret/port=3306）、**bcmath 扩展已验证可用**（金额整数运算依赖）
  - Node 24.18.0（web/，Vue 3.5 + Vite + Element Plus 2.14.4 + Pinia + Vue Router + axios）；Vitest 4 + @playwright/test 1.62（chromium 已装）
  - Python 3.14.6: `D:\code\envs\python\3.14.6\python.exe`（ui-ux-pro-max search.py 需完整路径调用）
  - CI 工具链（均已入库）：server/ — pint 1.30.5、php_codesniffer 4.0.4（PSR-12，phpcs.xml 含 5 条花括号豁免；**PHPCS 4 退出码：2=仅 warning（行长软限制）不阻断，CI 已 `|| PHPCS_STATUS=$?` 放行**）、larastan 3.10.0 + phpstan 2.2.8（level 5，需 `--memory-limit=1G`）；web/ — eslint 10 + typescript-eslint 8 + eslint-plugin-vue 10（flat config，**无 browser globals：禁用 setTimeout 用 nextTick**）、prettier 3、stylelint 17、husky 9 + lint-staged 17
- 关键配置:
  - `server/.env`（gitignore 不入库）: DB 已配 MySQL php_design；APP_URL=http://localhost:8000
  - `web/vite.config.ts`: 代理 `/api` → `http://localhost:8000`；端口 5173；vitest include 限定 `src/**/*.test.ts`
  - `web/src/main.ts`: Element Plus 已配 zh-cn locale
  - **playwright.config.ts 已设 `workers: 1`**（inventory/purchase/sales 共享库存数据必须串行，勿改回并行）；`retries: CI ? 2 : 0`；webServer 用 sqlite（`server/database/e2e.sqlite`，已 gitignore）自动 `migrate:fresh --seed`
  - **注意**：phpunit 测试库为 sqlite（phpunit.xml，**sqlite 外键约束开启**——测试插外键数据须先建真实主数据）；**sqlite 唯一冲突错误码 19 ≠ MySQL 1062**（撞号重试已双码兼容）；**sqlite 将 DECIMAL 编译为 NUMERIC**（Schema 类型断言须兼容 decimal/numeric）；**phpunit 权限用例须 `$this->app['auth']->forgetGuards()`**（同 app 实例 guard 缓存，CheckTest 先例）
- 安装/构建/启动命令:
  ```bash
  cd D:/code/project/php-design
  docker compose up -d                                   # MySQL 8.4
  cd server && php artisan migrate --seed && php artisan serve   # 后端 :8000
  cd ../web && npm run dev                               # 前端 :5173
  cd server && php artisan test && cd ../web && npx vitest run    # 全量测试
  # CI 门禁（本地模拟云端；phpcs 退出码 2=仅 warning 放行；门禁勿与 E2E 并行——会饿死 vite 响应）：
  cd web && npm run type-check && npm run lint && npm run lint:css && npm run format:check && npm run test:unit
  cd ../server && vendor/bin/phpstan analyse --no-progress --memory-limit=1G && vendor/bin/phpcs -q; S=$?; [ $S -ne 0 ] && [ $S -ne 2 ] && exit $S; vendor/bin/pint --test && php artisan test
  cd ../web && npx playwright test                        # E2E（自动起后端种子库+vite，43 用例，~2m）
  ```

## 4. 全局约定 (Locked Conventions)
- **目录结构**: `server/`（Laravel 13 API，`/api/v1/*`，routes/api.php）；`web/`（Vue 3 前端）；`docs/superpowers/specs|plans/`；`docs/test/`（E2E 测试文档）；`docs/progress/`（本进度文档，每模块一份）；`design-system/nexus-factory/`（MASTER.md + pages/{master-data,inventory,purchase,sales}.md 页覆盖）
- **SDD 实施流程（不可跳过）**: 每模块 = writing-plans 生成计划 → subagent-driven-development 逐 Task 执行（TDD、每 Task reviewer、ledger 在 `.superpowers/sdd/`，用完即删）→ E2E 文档全过 → 最终全分支审查 → **PR 合入 main**
- **git 合入流程**: main 分支保护（strict status check「CI 汇总（全部通过）」+ enforce_admins），直接 push main 被 GH006 拒绝——必须：`git checkout -b feat/xxx` → push 分支 → `gh pr create --base main` → `gh pr checks --watch` 云端 CI 全绿 → `gh pr merge --rebase --delete-branch` → 本地 `git fetch && git reset --hard origin/main` 对齐
- **接口契约**: 统一响应 `{code,message,data}`（code=0 成功；422 仅格式层，业务冲突一律走业务码）；错误码：1001-1008 系统管理、1101-1124 基础资料、1201-1206 库存、1301-1312 采购（1301 空明细/1302 数量≤0/1303 已审核订单不可改/1304 不可删/1305 重复审核/1306 不可关闭/1307 仓库库位必填/1308 超量或订单不可入库/1309 已审核入库单不可改删/1310 重复审核入库单/1311 负价/1312 重复商品）、**1401-1412 销售（1401 空明细/1402 已审核订单不可改/1403 不可删/1404 重复审核/1405 不可关闭/1406 仓库库位必填/1407 超剩余量+订单不可出库+客户一致性/1408 已审核出库单不可改删/1409 商品[X]库存不足，当前库存 {qty}/1410 重复审核出库单/1411 负价/1412 重复商品；数量≤0 与原料禁售走 422）**、1501-1528 生产、1601 报表；权限中间件 `permission:{资源}.{动作}`（approve/close 复用 update）；分页 `{items,total,page,per_page}`；per_page 钳制 `max(1,min(100,(int)$request->input('per_page',10)))`
- **库存引擎（核心不变式）**: 一切库存变动（采购入库/销售出库/领退料/成品入库/委外收发/盘点）**必须**经 `InventoryService::apply`（事务双写流水+余额；movements 每条含 product_id/warehouse_id/location_id/direction(1|-1)/quantity(恒正)/source_type 枚举/source_id/source_no/remark；出库超卖抛 `InventoryException`；余额允许 0 不允许负；流水只增不改不删）；余额=流水求和恒等式
- **单据审核事务模式（销售模块沉淀，生产模块照抄）**: 事务内「锁单行（幂等判重）→ 逐行锁关联单据行（剩余量/状态复核）→ **逐行锁余额行校验余额充足（出库类单据；消息含商品名与精确库存快照，尾零 `str_contains($current,'.') ? rtrim(rtrim($current,'0'),'.') : $current`）** → InventoryService::apply（同事务 savepoint）→ 回写累计量 → syncStatus 重算 → 置已审核+操作人+时间」；业务冲突抛模块专属 Exception（第二参数=业务码）由外层 catch 转 fail；**涉及金额用 bcmath 整数分运算**（bcmul/bcadd/bcsub/bccomp，quantity 允许 2 位小数、price 整数分、amount=round(q×p)）；validatePayload 中 quantity/price 加 `regex:/^-?\d+(\.\d{1,2})?$/`（防科学计数法 500，负值放行到业务码）
- **单号规则**: `{前缀}{yyyyMMdd}-{3位}` 统一走 `DocumentSequenceService::nextNo(type, prefix, persist, legacyMax)`（document_sequences 表，type+date 唯一，FOR UPDATE 原子取号，删除不回退，撞号 1062/19 重试，老库 max 衔接）；已有 type：check/bom/po/pi/so/sout（生产加 mo/ro 等）；**禁止 count+1**
- **引用保护（DeletionGuard）**: 已生效——inventory_balances（1106/1107）、inventory_movements（1116）、purchase_orders.supplier_id（1109）、purchase_inbounds supplier/warehouse/location（1109/1106/1107）、purchase_inbound_items.product_id（1116）、**sales_orders.customer_id（1111）、sales_outbounds customer/warehouse/location（1111/1106/1107）、sales_order_items/sales_outbound_items.product_id（1116）**；待生产表落地后自动激活——work_order_operations.process_id（1113）、production_orders.bom_id|product_id（1121）
- **RBAC**: 权限只授权给角色；新模块权限追加 RbacSeeder 权限数组（group=模块名），admin 自动全量持有、operator 自动持有全部 `%.list`；前端按钮用 `auth.has()` 门控
- **设计系统**: nexus-factory 为准（主色 #334155、强调绿 #059669、背景 #F8FAFC、Fira Code+Fira Sans）；页面骨架 `.page-card/.page-title/.toolbar/.btn-primary`；按钮文案全角空格「新 建/保 存/编 辑/删 除/审 核/关 闭/查 看/查 询」（E2E 按文案定位，正则 `/\s*建/` 双兼容半角）；金额列 Fira Code 右对齐 ¥（formatYuan 千分位）；订单五态标签（草稿灰/已审核绿/部分出库蓝/已完成深绿 tag-done/关闭红）；弹窗 900px；扫码自动聚焦（v-model + 命中清空/未命中保留）；页覆盖规范见 `pages/{inventory,purchase,sales}.md`
- **用户硬性约束（AGENTS.md）**: 全部注释/日志/文档中文；UTF-8 无 BOM；LF 行尾（.gitattributes 已强制）；无死代码（未使用 import 零容忍）；核心路径（认证/权限/**库存一致性/审核并发/金额 bcmath**）100% 单测；E2E 未过不合并
- **工程纪律（本会话教训）**: 严禁手动运行 pint/prettier（pre-commit 钩子 lint-staged 自动修复属正常）；SDD 派发 implementer 时明确「不要运行 pint/prettier、不要 git add -A」；**Task 提交前必跑全量 phpstan（0 错误）——销售模块 Task 3/4 implementer 跳过导致 13 错误遗留到最终门禁（1 轮修复循环）**；**门禁勿与 E2E 并行（同机 CPU 争用会饿死 vite → login 30s 超时）**；phpunit 权限用例须 forgetGuards；计划文件生成用 Write/Edit 分块（heredoc 易截断）；el-input 组件实例无 value 属性（扫码取值必须 v-model）；eslint 无 browser globals（禁用 setTimeout，用 nextTick）；edit 流程必须区分 create/update 走不同 API
- **云端 CI**: `.github/workflows/ci.yml` 在 push main / PR 到 main 时跑后端（Pint→PHPCS→PHPStan→PHPUnit）+ 前端（vue-tsc→ESLint→Stylelint→Prettier→Vitest）+ E2E（Playwright，retries 2）+ 汇总 job；main 分支保护要求「CI 汇总（全部通过）」strict 检查
- **环境杂项**: 本机偶发内存占用来自 MindSoar 项目的 basedpyright 检查（与 php-design 无关，E2E login 偶发超时诱因之一）；`.zcode/` 与 `auth.json` 已 gitignore
