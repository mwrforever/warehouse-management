# 2026-08-12 生产管理模块端到端测试

> **对应细化 spec：** `docs/superpowers/specs/2026-08-12-production-spec.md`
> **执行工具：** playwright-cli；**前置依赖：** 系统管理 + 基础资料 + 库存 + 采购 + 销售模块测试通过（BOM/工序/商品/仓库齐备，库存基线可用）。

## 1. 测试概述与业务流转

生产模块是全系统最复杂链路，按业务顺序执行完整生产闭环：

```
建工单(FIN-002×10, BOM 展开) → 下达 → 开工 → 领料(原料-验证) → 工序1报工
→ 工序2委外(发出-回收+) → 末工序报工完成 → 成品入库(成品+ → 工单自动完成)
→ 退料冲销 → 工单关闭 → 边界拦截(超领/超收/虚报) → 幂等验证
```

**测试专用数据**：工单产品 FIN-002（成品B）；BOM：MAT-001×2/成品（基础资料测试已建，若清理需重建）；工序：下料(seq1)、测试工序→正式命名为「组装」(seq2)、「质检」(seq3)（用基础资料测试的工序若已删则重建 2-3 个）；供应商 SUP-001（委外用）；仓库主仓。**库存基线**：执行前记录 MAT-001、FIN-002 余额 P₁、F₁。

## 2. 前置条件与数据准备

| 依赖项 | 要求 |
|---|---|
| 环境 | 前后端运行中；BOM(FIN-002, 启用版) 存在；工序 ≥3 个（下料/组装/质检，sort 1/2/3） |
| 库存 | MAT-001 ≥ 20（领料用）、FIN-002 初始任意；不足时采购入库补足 |
| 权限 | admin 含 `production.*` 全部 |
| 数据 | 每个用例前工单/单据可复用，注意单号流水递增 |

## 3. 测试用例

### TC-PRD-01 工单创建与 BOM 展开

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 「生产管理 → 工单」点「新 建」 | 弹窗打开 | 成品下拉仅成品商品；选 FIN-002 后自动校验启用 BOM |
| 2 | 填数量 10、计划日期今天，保存 | `POST /api/v1/production/orders`，请求体 `{"product_id":FIN002,"quantity":10,"bom_id":..}`；响应 `{"code":0,"data":{"no":"MO20260812-001"}}` | 列表出现 MO20260812-001 草稿 |
| 3 | 点「详 情」 | `GET /api/v1/production/orders/{id}` | 物料需求 tab：MAT-001 需求 **20**（10×2）；工序 tab：下料/组装/质检 3 行全「待开工」 |
| 4 | 新建工单选无 BOM 成品（SEMI-001 若无 BOM） | 响应 `{"code":1501,"message":"该成品没有启用版本的 BOM"}` | 拦截 |

**通过标准：** BOM 展开数量正确（数量×用量）、工序序列自动生成、无 BOM 拒绝。

### TC-PRD-02 下达与开工

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 对 MO20260812-001 点「下 达」 | confirm → `POST /api/v1/production/orders/{id}/release`，响应 `{"code":0,"data":{"warnings":[...]}}`（MAT-001 若充足则 warnings 空） | 状态「已下达」；若有缺料显示琥珀警告条 |
| 2 | 重复下达（fetch） | `{"code":1505,"message":"工单已下达"}` | 幂等拒绝 |
| 3 | 点「开 工」 | `POST /api/v1/production/orders/{id}/start` | 状态「生产中」；详情页工序步骤条：下料=进行中 |
| 4 | 重复开工 | `{"code":1506,...}` | 拒绝 |

**通过标准：** 下达/开工流转 + 幂等。

### TC-PRD-03 领料审核与发料（原料扣减）

**基线：** MAT-001 余额 P₁。

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 工单详情点「领 料」→ 弹窗选工单 | `GET /api/v1/production/picks/from-order/{id}` | 预填 MAT-001 需求 20、剩余 20 |
| 2 | 领用 20，仓库主仓/A-01，保存 | `POST /api/v1/production/picks`，响应 `{"code":0,"data":{"no":"PL20260812-001"}}` | 列表出现 PL20260812-001 草稿 |
| 3 | 改领用 25（>剩余 20） | 前端/后端 `{"code":1513,"message":"领料数量超过需求数量"}` | 超领拦截 |
| 4 | 恢复 20 保存 → 点「审 核」 | confirm → `POST /api/v1/production/picks/{id}/approve` | 审核成功 |
| 5 | 余额页查 MAT-001 | — | 数量 = **P₁ - 20** |
| 6 | 流水页筛「领料」 | `source_type=pick` | MAT-001 -20 流水，单号 PL20260812-001 |
| 7 | 点「发 料」 | `POST /api/v1/production/picks/{id}/issue`，响应 `{"code":0,"data":{"issue_status":"全部发料"}}` | 发料状态标签「全部发料」 |
| 8 | 工单详情物料 tab | — | MAT-001 已领 20、剩余 0 |

**通过标准：** 领料精确扣减、超领拦截、发料状态推进、需求回写。

### TC-PRD-04 工序报工与自动流转

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 报工页选 MO20260812-001 | — | 步骤条：下料=进行中 |
| 2 | 下料工序：合格 10、不良 0、工时 2.5，提交 | `POST /api/v1/production/operations/{op1}/reports`，请求体 `{"qualified_qty":10,"defective_qty":0,"hours":2.5}` | ElMessage「报工成功」；步骤条：下料=已完成、组装=进行中（**自动推进**） |
| 3 | 组装工序报工：合格 4、不良 0、工时 1 | POST | 组装未完成（累计 4<10），仍进行中 |
| 4 | 组装再报：合格 6、不良 0、工时 1 | POST | 组装自动「已完成」、质检=进行中 |
| 5 | 质检报工：合格 10、工时 0.5 | POST | 质检自动「已完成」 |
| 6 | 报工记录 tab | `GET /api/v1/production/operations/{op1}/reports` | 下料报工记录 1 条：合格 10/不良 0/工时 2.5 |

**通过标准：** 逐工序报工、累计达标自动推进、末工序完成后工单可完工。

### TC-PRD-05 报工边界拦截

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 质检工序（已完成）再报工 | `{"code":1509,"message":"该工序当前不可报工"}` | 已完成工序禁报 |
| 2 | 对进行中工序报合格 11（>计划 10） | `{"code":1510,"message":"合格数不能超过工单计划数量"}` | 拦截 |
| 3 | 合格 8 + 不良 5（合计 13>10） | `{"code":1511,...}` | 拦截 |
| 4 | 工时 -1 | `{"code":1512,...}` | 拦截 |

**通过标准：** 四类边界全部拦截（含虚报防呆）。

### TC-PRD-06 委外发出与回收

**前置：** 新建工单 MO20260812-002（FIN-002×5）；供应商 SUP-001。

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 「生产管理 → 委外」点「新 建」：选 MO20260812-002、委外工序=组装、供应商 SUP-001、数量 5、仓库主仓/A-01，保存 | `POST /api/v1/production/outsourcings`，响应 `{"code":0,"data":{"no":"OS20260812-001"}}` | 列表出现 OS20260812-001 草稿 |
| 2 | 点「审 核」（发出） | confirm → `POST /api/v1/production/outsourcings/{id}/approve` | 状态「已审核」；库存：FIN-002 -5（outsourcing_out 流水） |
| 3 | 点「回 收」：填回收量 5、仓库主仓/B-01，提交 | `POST /api/v1/production/outsourcings/{id}/receipts`，请求体 `{"quantity":5,...}` | 委外单状态「已回收」；库存：FIN-002 +5（outsourcing_in 流水，单号 OSR20260812-001） |
| 4 | 再回收 1（超收） | 响应 `{"code":1524,"message":"回收数量超过委外数量"}` | 超收拦截 |
| 5 | 对已回收单重复审核（fetch） | `{"code":1523,...}` | 幂等拒绝 |

**通过标准：** 委外发出-回收闭环、库存双变动精确、超收/幂等拦截。

### TC-PRD-07 成品入库与工单自动完成

**前置：** MO20260812-001 全部工序已完成、completed_qty=0。

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 工单详情点「成品入库」：成品 FIN-002 数量 10、仓库主仓/B-01，保存 | `POST /api/v1/production/finished-inbounds`，响应 `{"code":0,"data":{"no":"FI20260812-001"}}` | 列表出现 FI20260812-001 草稿 |
| 2 | 数量改 11（>剩余 10） | `{"code":1525,"message":"入库数量超过工单剩余产量"}` | 拦截 |
| 3 | 改 10 保存 → 点「审 核」 | confirm → `POST /api/v1/production/finished-inbounds/{id}/approve` | 审核成功 |
| 4 | 余额页查 FIN-002 | — | 数量 = F₁ **+10**（含委外测试后的基数，按当时记录） |
| 5 | 流水页筛「成品入库」 | `source_type=finished_inbound` | FIN-002 +10 流水，单号 FI20260812-001 |
| 6 | 工单列表刷新 | — | MO20260812-001 状态**自动「已完成」**，进度 100% |
| 7 | 重复审核 FI（fetch） | `{"code":1528,"message":"该成品入库单已审核"}` | 幂等拒绝 |

**通过标准：** 成品入库精确 +、满产自动完成、超量/幂等拦截。

### TC-PRD-08 完工校验与关闭

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 新建工单 MO20260812-003（FIN-002×5）→ 下达 → 不报工直接调 complete | `{"code":1507,"message":"存在未完成工序，无法完工"}` | 工序未完成禁完工 |
| 2 | 完成全部工序但不做成品入库，调 complete | `{"code":1508,...}`（无成品入库禁完工） | 拦截 |
| 3 | 对 MO20260812-001 点「关 闭」 | `POST /api/v1/production/orders/{id}/close` | 状态「关闭」 |
| 4 | 对已关闭工单调 release/start | `{"code":1503/1505,...}` | 关闭后无操作入口 |

**通过标准：** 完工双前置校验、关闭封口。

### TC-PRD-09 退料冲销

**前置：** MO20260812-001 已领 MAT-001 20。

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 「生产管理 → 退料」新建：选工单 MO20260812-001、MAT-001 数量 2、仓库主仓/A-01，保存 | `POST /api/v1/production/returns`，响应 `{"code":0,"data":{"no":"RL20260812-001"}}` | 列表出现 |
| 2 | 数量改 25（>已领 20） | `{"code":1517,"message":"退料数量超过已领数量"}` | 拦截 |
| 3 | 改 2 保存 → 审核 | `POST /api/v1/production/returns/{id}/approve` | 库存 MAT-001 **+2**（return 流水）；工单物料 tab 已领 20→**18**（冲销） |
| 4 | 重复审核 | `{"code":1519,...}` | 拒绝 |

**通过标准：** 退料精确冲销（库存+、已领-）。

### TC-PRD-10 领料库存不足拦截

**前置：** 使 MAT-001 余额 < 需求（用销售出库或盘点清至 <20，用库存模块工具）。

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 新建领料单（MO20260812-003 需求 MAT-001 10），保存草稿 | `{"code":0}` | 草稿可存 |
| 2 | 点「审 核」 | `{"code":1515,"message":"商品[MAT-001]库存不足"}`，整体回滚 | 审核失败、余额不变、无流水 |
| 3 | 恢复 MAT-001 库存后重新审核 | `{"code":0}` | 恢复正常 |

**通过标准：** 库存不足双端拦截 + 事务回滚。

## 4. 失败处理说明（测试未通过时）

同库存/采购模块 §4。本模块特有判定：

- **PRD-03/07/09 库存数不符**：核心缺陷（InventoryService 方向/事务），先查事务包裹与方向常量
- **PRD-04 工序不自动推进**：报工服务状态机缺陷（累计判定/下一工序推进逻辑）
- **PRD-07 工单不自动完成**：成品入库后 completed_qty 回写与状态判定缺陷
- **PRD-06 委外回收不联动**：回收单双写（流水+委外状态）事务缺陷
- 修复流程：superpowers `systematic-debugging` 定位 → `test-driven-development` 补 PHPUnit（生产服务 100% 覆盖：状态机流转/库存双写/事务回滚）→ 全量回归本文档 + 库存模块恒等式

## 5. 测试结果记录模板

| 用例 | 结果（通过/失败） | 失败层级 | 失败详情 | 修复引用 |
|---|---|---|---|---|
| TC-PRD-01 | | | | |
| ... | | | | |
