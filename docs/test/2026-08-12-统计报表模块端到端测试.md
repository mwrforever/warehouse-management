# 2026-08-12 统计报表模块端到端测试

> **对应细化 spec：** `docs/superpowers/specs/2026-08-12-report-spec.md`
> **执行工具：** playwright-cli；**前置依赖：** 系统管理 + 基础资料 + 库存 + 采购 + 销售 + 生产模块测试全部通过（报表数据源来自这些模块已产生的真实业务数据）。

## 1. 测试概述与业务流转

报表为只读聚合，测试核心是**数字口径与源数据一致**。执行前记录上一批模块测试留下的已知数据快照，逐报表核对：

```
登录(admin) → 库存报表(与余额页核对) → 出入库汇总(与流水求和核对)
→ 生产统计(与工单/报工核对) → 采购销售汇总(与单据金额核对) → 日期边界 → 空态
```

**数据快照（测试执行前从各模块记录）**：

| 数据项 | 值（以实际为准） | 来源 |
|---|---|---|
| MAT-001 余额 | P₁ | 库存余额页 |
| 近 30 天入库总量/出库总量 | Σin / Σout | 库存流水按方向求和 |
| 已完工工单 MO20260812-001 | 计划 10 / 完工 10 / 合格 10 / 不良 0 / 工时 5 | 生产模块 |
| 已审核采购金额合计 / 销售金额合计 | PA / SA | 采购/销售模块单据 |

## 2. 前置条件与数据准备

| 依赖项 | 要求 |
|---|---|
| 环境 | 前后端运行中；六个上游模块测试已通过且数据未被清理 |
| 权限 | admin 含 `report.*` |
| 数据 | 至少存在：≥1 个已审核采购入库、≥1 个已审核销售出库、≥1 个已完成工单（含报工记录） |

## 3. 测试用例

### TC-RPT-01 库存报表维度一致性

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 「统计报表 → 库存报表」默认「按分类」 | `GET /api/v1/reports/inventory-summary?group_by=category` | 原材料分类行数量 = MAT-001 余额（P₁，同仓库求和）；KPI「库存总量」= 全部商品余额之和，与余额页手动加总一致 |
| 2 | 切换到「按类型」 | 请求带 `group_by=type` | 「原料」行数量与余额页 type=raw_material 筛选总数一致 |
| 3 | 切换到「按仓库」 | `group_by=warehouse` | 主仓行数量 = 主仓下所有库位余额合计 |

**通过标准：** 三个维度数字均与库存模块源数据精确一致。

### TC-RPT-02 出入库汇总与流水求和

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 出入库汇总选近 30 天、粒度「日」 | `GET /api/v1/reports/movements-summary?date_from=..&date_to=..&granularity=day` | 每天行的 inbound_qty/outbound_qty = 该日流水按方向求和；KPI 总入库量=Σin、总出库量=Σout（用 run-code 调 movements 接口按 created_at 分日核对） |
| 2 | 粒度切「月」 | `granularity=month` | 每月行数=该月求和，totals 不变 |
| 3 | 净变动 KPI | — | = 总入库量-总出库量 |
| 4 | 点击某行（如今天） | 跳转 `库存流水` 页并预填日期 | URL 含 date_from/date_to=该日，列表为当日流水 |

**通过标准：** 聚合口径与流水逐条求和一致、下钻正确。

### TC-RPT-03 生产统计-达成率与良率

**前置：** MO20260812-001 计划 10、完工 10、合格 10、不良 0、工时 5（生产模块测试数据）。

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 「生产统计」选日期范围含工单计划日期 | `GET /api/v1/reports/production` | 表格出现 MO20260812-001 行：达成率 **100.00%**（绿色）、良率 **100.00%**（无不良）、总工时 5 |
| 2 | 展开「物料耗用」列 | — | 明细含 MAT-001：used_qty = **Σ领料-Σ退料** = 20-2 = 18（生产模块实际数据） |
| 3 | 用 run-code 核对：领料流水求和 - 退料流水求和 | `source_type=pick` 与 `source_type=return` | 与报表 used_qty 一致 |
| 4 | 颜色分级验证 | — | 达成率≥100% 行绿色标签；若存在 <80% 工单（如未完工的 MO-003）显示红色 |

**通过标准：** 达成率/良率/工时/耗用四指标与生产模块源数据精确一致、颜色分级正确。

### TC-RPT-04 采购销售汇总

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 「采购销售汇总」选日期范围、粒度「月」 | `GET /api/v1/reports/purchase-sales` | 本月行 purchase_amount = 已审核采购入库单金额合计（含 PO 系列单）；sales_amount = 已审核销售出库单金额合计 |
| 2 | 用 run-code 调 inbounds/outbounds 列表接口累加 total_amount 核对 | — | 分转元无误差（金额两位小数） |
| 3 | 差额 KPI | — | = 销售金额-采购金额（可负，显示红色负数） |
| 4 | 切换粒度「日」 | — | 每日拆分正确，totals 不变 |

**通过标准：** 金额口径与单据合计精确一致。

### TC-RPT-05 日期边界与空态

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | 出入库汇总选未来日期（如 2099-01-01 至 2099-01-31；daterange 双输入框直接 fill + Enter 确认） | 响应 `items:[]`、totals 全 0 | 页面显示空态：「暂无数据」+ 图表空白，无报错、无 loading 卡死（断言 KPI 0.00 + el-empty + 表格空文案） |
| 2 | 生产统计选未来日期 | 同上 | 空态正常（API 断言 items=[]） |
| 3 | 用 run-code 调 `date_from=2099-01-31&date_to=2099-01-01`（倒置） | 响应 `{"code":1601,"message":"开始日期不能晚于结束日期"}` | 参数校验拦截 |
| 4 | 前端选择器限制倒置（若 el-date-picker 自动交换则跳过） | — | 记录实际行为 |

**通过标准：** 空态三要素（KPI 0/图空/文案）齐全、倒置日期被拦。

### TC-RPT-06 权限控制

| # | 操作 | 预期请求与响应 | 通过条件 |
|---|---|---|---|
| 1 | admin 用 API 创建 operator 用户（挂「操作员」角色，仅 *.list），登出后 operator 登录 | — | 侧边栏「统计报表」菜单组内 4 个报表条目（库存报表/出入库汇总/生产统计/采购销售汇总）隐藏（组标题为静态分组头无条件渲染，断言以组内条目为准） |
| 2 | fetch 报表接口 | 响应 `{"code":403,...}` | 后端拦截 |

**通过标准：** 报表只读 + RBAC 双端控制。

## 4. 失败处理说明（测试未通过时）

同前序模块 §4。本模块特有判定：

- **数字不一致（RPT-01~04）**：先判定是**聚合 SQL 缺陷**还是**源数据缺陷**——用 run-code 直接对源表聚合核对；若源数据本身违反恒等式，问题在上游模块，按上游模块修复流程处理并联动回归
- **口径偏移**（如达成率用错分子/分母）：生产统计查询缺陷，补 PHPUnit 报表服务测试后修复
- 修复流程：superpowers `systematic-debugging` → `test-driven-development` → 回归本文档；若源数据问题需同时回归对应上游模块测试文档

## 5. 测试结果记录

**执行环境：** 本地全量 E2E（`npx playwright test`，webServer 自动重建 SQLite 种子库 + vite），统计报表 spec 跑在 production/purchase/sales 之后、system 之前（文件名 `stats-report.spec.ts`，字典序锁定）。执行日期 2026-08-13，**60/60 用例全部通过**（既有 54 + 新增 6）。

| 用例 | 结果（通过/失败） | 失败层级 | 失败详情 | 修复引用 |
|---|---|---|---|---|
| TC-RPT-01 | 通过 | — | — | — |
| TC-RPT-02 | 通过 | — | — | — |
| TC-RPT-03 | 通过 | — | — | — |
| TC-RPT-04 | 通过 | — | — | — |
| TC-RPT-05 | 通过 | — | — | — |
| TC-RPT-06 | 通过 | — | — | — |

**按实际可达流程的修正记录（实现与文档初稿差异，已按 E2E 实际流程修正并加注）：**

1. **TC-RPT-01**：`categories` 接口返回树形数组（顶层节点 + 可选 children，无 items 包装）——测试先递归扁平化为 id→name 映射再反查分类 id 计算期望值；文档步骤不变。
2. **TC-RPT-02**：下钻断言改为「URL 正则（`/inventory/movements?date_from=..&date_to=..`）+ 流水页 daterange 双输入框值 = 该日 + 当日流水行出现」；原「日期文本可见」断言因日期显示在 input value 中（非文本节点）不可达。
3. **TC-RPT-05**：daterange 未来日期填充用「双输入框直接 fill + Enter 确认」（`keyboard.type` 逐字键入焦点会落入面板内部输入框、确认不稳定，实测不可靠）；空态三要素断言 = KPI 显示 0.00 + el-empty 出现 + 表格空文案。步骤 4（前端选择器倒置）本轮未实测 UI 路径——倒置拦截由 API 直调 1601 锁定（el-date-picker 本身不阻止任意区间选择，倒置请求由后端 1601 拦截）。
4. **TC-RPT-06**：菜单组标题「统计报表」为静态分组头无条件渲染（与其余菜单组一致），operator 登录后组内 4 个报表条目隐藏——断言以组内条目为准（文档步骤已同步修正）；operator 用户经 API 直建（UserStoreRequest 的 status 为必填字段，载荷含 status:1）。
